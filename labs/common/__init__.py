"""labs.common — helpers compartidos por los benches de labs/.

Los scripts lo importan insertando la raíz del repo en sys.path:

    sys.path.insert(0, str(REPO_ROOT))
    from labs.common import env, fal, images, sam, report
"""
