{
  "$comment": "Fronteras arquitectónicas de ne-fan, verificadas por test/architecture.test.ts. Cada regla sustituye a un invariante que antes solo vivía como prosa en CLAUDE.md. severity=error: verde hoy, no puede volver a romperse. severity=warn: deuda YA existente, congelada en `max` — el test falla si CRECE. Toda excepción lleva motivo obligatorio. El `ignore` es por NOMBRE de directorio y cubre además los árboles REGENERABLES que ningún escaneo debe recorrer: `capturas` (qa/capturas/<corrida>/ — desde 2026-08-27 se acumula por corrida en vez de vaciarse, y una sola deja ~75 ficheros), `.tmp`, `coverage` y `reports`. Nada de ahí se recolecta nunca, y recorrerlo lo paga cada `npm test`.",
  "scan": {
    "roots": [
      {
        "dir": "nefan-core/src",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-core/bridge",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-core/services",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-core/scripts",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-html/src",
        "ext": [
          ".ts",
          ".css"
        ]
      },
      {
        "dir": "ai_server",
        "ext": [
          ".py"
        ]
      },
      {
        "dir": "narrative-mcp",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-core/test",
        "ext": [
          ".ts"
        ]
      },
      {
        "dir": "nefan-core/data",
        "ext": [
          ".json",
          ".md"
        ]
      },
      {
        "dir": "labs",
        "ext": [
          ".ts",
          ".mjs",
          ".json"
        ]
      },
      {
        "dir": "qa",
        "ext": [
          ".mjs"
        ]
      }
    ],
    "files": [
      "start.sh",
      "nefan-html/vite.config.ts"
    ],
    "ignore": [
      "node_modules",
      "dist",
      ".venv",
      "__pycache__",
      "archivo",
      "capturas",
      ".tmp",
      "coverage",
      "reports"
    ]
  },
  "rules": [
    {
      "id": "html-no-importa-core-con-node",
      "desc": "El cliente 2D no puede importar los módulos del core que usan node:fs/path/crypto",
      "why": "La frontera browser-safe la sostiene hoy solo una separación deliberada de ficheros (games/style-application-schema vs style-application). Un import nuevo mete node:fs en el bundle de Vite y rompe el cliente en tiempo de carga. `narrative-state` entra en la lista el día que empieza a importar `plugins/hash` (node:crypto) para comprobar que el manifest embebido de un record migrado es de verdad el de su id: hasta entonces solo arrastraba `session-storage` como tipo, que se borra al compilar, y era importable del navegador por accidente. Nadie lo importa hoy desde el cliente, así que la regla nace verde; está aquí para que el día que alguien lo intente lo pare el checker y no un bundle roto en tiempo de carga.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "games/loader(\\.js)?$",
          "games/style-application(\\.js)?$",
          "games/vocabulary(\\.js)?$",
          "games/world-snapshot(\\.js)?$",
          "narrative/narrative-state(\\.js)?$",
          "narrative/session-storage(\\.js)?$",
          "plugins/loader(\\.js)?$",
          "contracts/common(\\.js)?$"
        ]
      }
    },
    {
      "id": "core-sin-three",
      "desc": "nefan-core no importa three.js",
      "why": "Lógica en nefan-core, los clientes solo pintan. El core debe poder correr en Node sin WebGL.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "^three($|/)"
        ]
      }
    },
    {
      "id": "core-sin-dom",
      "desc": "nefan-core no toca el DOM (document/window)",
      "why": "Lógica en nefan-core, los clientes solo pintan. Un acceso al DOM ata el core al navegador y rompe el bridge y los tests.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts"
      ],
      "text": {
        "pattern": "^(?!\\s*(?://|\\*|/\\*))[^\\n]*\\b(?:document|window)\\."
      }
    },
    {
      "id": "core-src-no-importa-bridge-ni-cliente",
      "desc": "src/ no importa de bridge/, services/ ni del cliente 2D",
      "why": "La dirección de dependencia es src ← bridge y src ← services. Invertirla convierte la lógica compartida en lógica de servidor.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "(^|/)bridge/",
          "(^|/)services/",
          "nefan-html"
        ]
      }
    },
    {
      "id": "core-puro-sin-node",
      "desc": "Los módulos de lógica pura del core no importan node:*",
      "why": "Son los que consume el cliente 2D en el navegador Y los que se mutan en el hardener. Un node:fs aquí rompe el bundle y saca al módulo del alcance de mutation testing. `src/session/**` entra el 2026-08-25 con session-facets.ts: colapsa el estado de sesión del CLIENTE en un valor, así que lo importa el bundle del navegador — un node:* ahí lo rompe en tiempo de carga.",
      "severity": "error",
      "files": [
        "nefan-core/src/combat/**/*.ts",
        "nefan-core/src/scene/**/*.ts",
        "nefan-core/src/simulation/**/*.ts",
        "nefan-core/src/store/**/*.ts",
        "nefan-core/src/world-map/**/*.ts",
        "nefan-core/src/protocol/**/*.ts",
        "nefan-core/src/contracts/**/*.ts",
        "nefan-core/src/contract/**/*.ts",
        "nefan-core/src/systems/**/*.ts",
        "nefan-core/src/session/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "^node:"
        ]
      }
    },
    {
      "id": "core-puro-sin-process-ni-fetch",
      "desc": "Los módulos de lógica pura del core no leen process.* ni llaman a fetch()",
      "why": "Pureza real: entrada -> salida. La configuración entra por parámetro y la red vive en bridge/ (undici, ws).",
      "severity": "error",
      "files": [
        "nefan-core/src/combat/**/*.ts",
        "nefan-core/src/scene/**/*.ts",
        "nefan-core/src/simulation/**/*.ts",
        "nefan-core/src/store/**/*.ts",
        "nefan-core/src/world-map/**/*.ts",
        "nefan-core/src/protocol/**/*.ts",
        "nefan-core/src/contracts/**/*.ts",
        "nefan-core/src/contract/**/*.ts",
        "nefan-core/src/systems/**/*.ts"
      ],
      "text": {
        "pattern": "^(?!\\s*(?://|\\*|/\\*))[^\\n]*(?:(?<![\\w-])process\\.|\\bfetch\\s*\\()"
      }
    },
    {
      "id": "html-sin-node-ni-rutas-crudas",
      "desc": "El cliente 2D no importa node:* ni alcanza el core por ruta relativa",
      "why": "El único punto de acoplamiento con el árbol del core es el alias @nefan-core (vite.config.ts + tsconfig paths). Una ruta relativa se salta el alias y rompe el build.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "^node:",
          "^\\.\\..*nefan-core"
        ]
      }
    },
    {
      "id": "three-solo-en-fps-gl",
      "desc": "three.js solo se importa en fps-gl.ts, el único módulo que lo posee",
      "why": "three entra por import dinámico para no engordar el bundle base; un import suelto en otro fichero lo mete en el arranque. Y hay una razón más fuerte desde que el cliente tiene UNA vista: cada importador de three tiende a abrir su propio WebGLRenderer, y un segundo contexto WebGL en la pestaña compite por la GPU con el mundo que el jugador está mirando. Esta regla absorbe a `fps-no-usa-el-singleton-offscreen`, que prohibía a la fps tocar el renderer singleton offscreen del clay (el del repintado y los thumbnails): ese singleton murió con la vista oblicua y la regla se quedó sin sujeto, pero su porqué —el contexto WebGL es un recurso único y con dueño— es exactamente el de aquí.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "imports": {
        "forbid": [
          "^three($|/)"
        ]
      },
      "exceptions": [
        {
          "path": "nefan-html/src/renderer/fps-gl.ts",
          "reason": "renderer y canvas del mundo — el único contexto WebGL del cliente"
        }
      ]
    },
    {
      "id": "cliente-no-convierte-celdas-a-metros",
      "desc": "El cliente no interpreta Format D crudo (terrain_legend / meters_per_cell / primitivas del esquema)",
      "why": "El bridge normaliza con formatDToWorld y el cliente pinta world scene en metros. Si el cliente porta la conversión hay dos formatos que mantener y divergirán, y la segunda copia se entera tarde de cada cambio del primero. Las PRIMITIVAS del esquema entran con la tanda del bosque (#243 #233 #232): el cliente las leía para componer el plan del tile por su cuenta, que es la misma frontera por el otro lado — no convertía celdas a metros, pero sí derivaba geometría del crudo, y su bosque podía no ser el del bridge. Hoy el plan viaja RESUELTO en la world scene (`__plan`) y el cliente lo lee. De las dos primitivas que nombraba este patrón solo queda `vegetation_zones`: la de salas se retiró entera con #301 y su token vive ahora en `campos-retirados-no-vuelven`, que cubre los cinco procesos y no solo el cliente.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\b(?:terrain_legend|meters_per_cell|vegetation_zones)\\b"
      }
    },
    {
      "id": "core-sin-catch-silencioso",
      "desc": "nefan-core no tiene catch vacío ni .catch(() => {})",
      "why": "Fail-loud uniforme: un error tragado en la lógica compartida se manifiesta como comportamiento raro en los dos clientes, sin traza. `nefan-core/scripts` entra el 2026-09-01 (revisión de fail-loud, medido a cero): son las herramientas que CALCULAN la deuda y los snapshots (crap, deuda, mutación, dump-config) — un catch vacío ahí no rompe el juego, corrompe la MEDIDA, que es peor porque nadie desconfía de un número verde. Hasta ese día el árbol (4.086 líneas) no estaba ni en scan.roots: cualquier regla podía citarlo como cubierto sin que ninguna lo mirase.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts"
      ],
      "text": {
        "pattern": "catch\\s*(?:\\([^)]*\\))?\\s*\\{\\s*(?://[^\\n]*\\s*|/\\*[\\s\\S]*?\\*/\\s*)*\\}|\\.catch\\(\\s*\\(\\s*\\)\\s*=>\\s*\\{\\s*\\}\\s*\\)"
      }
    },
    {
      "id": "narrative-mcp-sin-catch-silencioso",
      "desc": "narrative-mcp no tiene catch vacío ni .catch(() => {})",
      "why": "Mismo patrón que `core-sin-catch-silencioso`, sobre el proceso donde tragarse un error sale MÁS caro: narrative-mcp es el cable entre ai_server y el terminal del motor, y un error sin canal ahí no se manifiesta como error en ninguna parte — ai_server espera su timeout de 120–180 s y el jugador mira un loader eterno sin que ningún log diga por qué. Era, medido el 2026-09-01, el único árbol TS del repo SIN regla de fail-loud, y contenía las 3 únicas violaciones literales del patrón en todo el repo: el catch que envolvía `handleClientMessage` ENTERO (cualquier excepción de enqueueRequest —o del emisor de status que existía entonces, retirado en #343— desaparecía como si fuera un frame malformado), el probe de takeover (parse legítimo, cuerpo vacío ilegítimo — hoy es un tryParse que devuelve null solo para «no era JSON») y el `.catch(() => {})` de postProgress. Las tres se arreglaron el día que entró la regla, que por eso nace verde y con severity error. El canal de esta capa es `console.error` — stdout es el protocolo MCP y no se puede escribir en él.",
      "severity": "error",
      "files": [
        "narrative-mcp/**/*.ts"
      ],
      "text": {
        "pattern": "catch\\s*(?:\\([^)]*\\))?\\s*\\{\\s*(?://[^\\n]*\\s*|/\\*[\\s\\S]*?\\*/\\s*)*\\}|\\.catch\\(\\s*\\(\\s*\\)\\s*=>\\s*\\{\\s*\\}\\s*\\)"
      }
    },
    {
      "id": "handlers-sin-servidor",
      "desc": "Los handlers del State API y su despacho no conocen el transporte HTTP",
      "why": "Es el criterio de aceptación de #225 convertido en checker. `handle` era una cadena de 441 líneas con ocho parámetros posicionales que empezaban por `IncomingMessage`: para ejercer una sola ruta había que abrir un puerto, y por eso las tres tandas que iban a editar ahí (#179, #195, #224) habrían aterrizado todas en el mismo fichero. La definición FALSABLE de «probable sola» es que la pieza se invoque sin levantar un servidor, y hace falta un checker porque `bridge/` NO está en el reparto de data/contract/mutation-targets.json: no hay score de mutación que lo demuestre por nosotros. El patrón caza las cuatro formas de volver a atarlo al transporte —importar node:http, nombrar IncomingMessage o ServerResponse, o llamar a createServer—; la primera es también un import, pero el motor de reglas solo admite `imports` o `text`, nunca los dos, y como `node:http` aparece igualmente en el texto del import, el patrón de texto las cubre todas. `state-http-server.ts` NO entra en `files`: ahí es donde el transporte tiene que vivir, y es su único sitio.",
      "severity": "error",
      "files": [
        "nefan-core/bridge/state-http/**/*.ts"
      ],
      "text": {
        "pattern": "node:http|\\bIncomingMessage\\b|\\bServerResponse\\b|\\bcreateServer\\s*\\("
      }
    },
    {
      "id": "cadena-de-migracion-unica",
      "desc": "Solo src/plugins/migrate.ts ejecuta la cadena migrate[v] (runMigrationStep)",
      "why": "Un plugin evoluciona por dos caminos —el manifest del disco sube de versión en resume, y el motor narrativo manda un `plugin_register` con versión mayor (issue #164)— y la política del salto (bump real, una entrada `migrate` por cada versión intermedia, nunca degradar) tiene que ser LA MISMA, con el mismo texto de rechazo. Mientras vivió entera dentro de loader.ts, resolver el caso runtime pedía a gritos un `if` local en register.ts: dos jueces del mismo manifest que darían veredictos distintos sobre el mismo salto, y nadie se enteraría hasta que un save quedara a medias con un slice convertido a un shape que sus reglas ya no leen. Ahora la decisión vive en migrate.ts (módulo puro) y montar un segundo juez exige llamar a runMigrationStep, que es exactamente lo que esta regla corta. Los tests quedan fuera del alcance a propósito: test/plugin-migrate.test.ts lo llama para probar que es slice-only y puro (el contrato del DSL, no la política de versiones que lo usa) y test/architecture.test.ts necesita el literal para probar esta regla en negativo. `nefan-core/scripts` entra el 2026-09-01 (medido a cero): un «script de migrar saves» que llamara a runMigrationStep por su cuenta es exactamente el segundo juez que esta regla corta, y hasta ese día el árbol no se escaneaba. Lo que esta regla NO es, dicho para que nadie se confíe (QA, 2026-08-23): corta la CADENA, no la POLÍTICA. Un `if (manifest.version <= prior.version) throw …` copiado en register.ts no la dispara. Se estudió ampliarla a las comparaciones de versión y se descartó a propósito: en migrate.ts la comparación se escribe `to < from` sobre variables locales, así que cualquier patrón de texto que buscara `version` lo esquivaría un copista con dos `const`, y un candado que parece cubrir lo que no cubre es peor que no tenerlo. El argumento por el que basta: un pre-chequeo local solo podría RECHAZAR de más, nunca aprobar un salto que la cadena rechaza, porque el slice migrado sigue teniendo que salir de migratePluginSlice; para cambiar de verdad el veredicto hay que reimplementar el bucle, y eso exige runMigrationStep, que es justo lo que aquí se corta. Lo que vigila que los dos caminos digan lo MISMO son los tests de paridad de test/plugin-migrate.test.ts, que comparan el mensaje carácter a carácter.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts"
      ],
      "text": {
        "pattern": "\\brunMigrationStep\\b"
      },
      "exceptions": [
        {
          "path": "nefan-core/src/plugins/dsl/evaluate.ts",
          "reason": "define runMigrationStep — es el intérprete de una tanda de efectos sobre el slice, no la política de versiones que decide si el salto es legal"
        },
        {
          "path": "nefan-core/src/plugins/migrate.ts",
          "reason": "es el juez único del salto de versión: el resume (loader.ts) y plugin_register (register.ts) le preguntan a él y solo traducen su rechazo al error de su operación"
        }
      ]
    },
    {
      "id": "python-sin-error-con-200",
      "desc": "Los endpoints no devuelven {\"error\": ...} con 200 OK",
      "why": "FastAPI debe fallar con HTTPException para que el cliente distinga error de respuesta vacía. Hoy hay 70 HTTPException y ningún return de error.",
      "severity": "error",
      "files": [
        "ai_server/**/*.py"
      ],
      "text": {
        "pattern": "return\\s*\\{\\s*[\"']error[\"']"
      },
      "exceptions": [
        {
          "path": "ai_server/llm_client.py",
          "reason": "no es un handler HTTP: es el cliente WS del bridge y su dict de estado sí lleva campo error, con log"
        }
      ]
    },
    {
      "id": "python-sin-request-crudo",
      "desc": "Los endpoints del ai_server declaran su cuerpo con un BaseModel, no leen `request.json()`",
      "why": "Hermana de `python-sin-error-con-200`, y cubre justo lo que aquella NO ve. Aquella prohíbe el 200-con-error; esto prohíbe la AUSENCIA de contrato de entrada, que es la otra mitad de la convención de CLAUDE.md («Pydantic BaseModel por endpoint para que campos ausentes salgan como 422 estructurado») y hasta hoy era prosa sin candado.\n\nLO QUE CAZA, exactamente: un parámetro anotado `: Request` (el `Request` crudo de FastAPI) y un `await …request.json()`. Con eso basta porque las dos van siempre juntas: sin `Request` no hay de dónde sacar el json, y un `Request` que no se lee no sirve de nada.\n\nPOR QUÉ EXISTE, medido (#366). `/skin_sprite_sheet` era el ÚNICO endpoint del proceso sin modelo: `async def skin_sprite_sheet_endpoint(request: Request)` con `body = await request.json()` y SEIS `str(body.get(...))`. De los seis campos, dos tenían guarda —a mano, con `HTTPException(400)`— y cuatro no: un `styel_id` mal escrito quedaba en `\"\"`, el personaje se pintaba sin el estilo del juego y la factura se pagaba igual, sin una sola queja. El resto del ai_server es la capa más limpia del repo (70+ HTTPException, cero 200-con-error), así que este gate NACE A CERO REAL: hoy su único ocupante es la exención escrita de abajo, no una deuda congelada. Es `error` y no `warn` por eso — un `max` sobre un conteo que ya es cero autorizaría a volver a subirlo.\n\nLO QUE NO CAZA, dicho para que nadie lo cite como garantía: un `BaseModel` sin restricciones (`model: str` acepta `\"\"` igual que el `str(body.get(...))` de antes). El 422 lo da la AUSENCIA del campo; que un campo presente pero vacío también falle es cosa de sus `StringConstraints`, y eso no lo puede ver un patrón de texto. Tampoco mira los routers de fuera de `ai_server/routers/**` — el State API del bridge es TypeScript y tiene su propia frontera.",
      "severity": "error",
      "files": [
        "ai_server/routers/**/*.py"
      ],
      "text": {
        "pattern": ":\\s*Request\\b|await\\s+[A-Za-z_.]*request\\.json\\s*\\("
      },
      "exceptions": [
        {
          "path": "ai_server/routers/asset_proxy.py",
          "reason": "no tiene cuerpo que modelar: reenvía BYTES al asset-store tal cual (método, ruta, query y headers), y para eso necesita la petición entera. Un BaseModel aquí describiría un json que no existe."
        }
      ]
    },
    {
      "id": "html-sin-catch-silencioso",
      "desc": "El cliente 2D no traga errores en silencio",
      "why": "Fail-loud: todo catch recuperable debe pasar por errors.push(...) del error-log. Deuda existente congelada; cada PR que baje el número debe bajar también el max. Baja de 5 a 4 al darle canal al re-disparo del atlas fps (fps-atlas.ts): era el único de los cinco sin una línea que dijera qué se estaba tragando, y se comía la ÚLTIMA oportunidad de texturar un tile. Los cuatro que quedan son degradaciones documentadas en su propio catch (localStorage ausente, frame de sprite fuera de rango, blob podado, busto sin skin): bajar de 4 no es tocar el número, es decidir que esas cuatro merecen entrada en el log del jugador.",
      "severity": "warn",
      "max": 4,
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "catch\\s*(?:\\([^)]*\\))?\\s*\\{\\s*(?://[^\\n]*\\s*|/\\*[\\s\\S]*?\\*/\\s*)*\\}|\\.catch\\(\\s*\\(\\s*\\)\\s*=>\\s*\\{\\s*\\}\\s*\\)"
      }
    },
    {
      "id": "html-sin-promesa-muda",
      "desc": "El cliente no descarta una promesa sin canal de error (void promesa)",
      "why": "Fail-loud: una promesa descartada con `void` no tiene catch, no deja entrada en el error-log y el cliente NO tiene handler de `unhandledrejection` — el rechazo se pierde entero. `html-sin-catch-silencioso` no la ve: aquella caza `catch` VACÍOS, y aquí no hay catch ninguno. Por eso #181 vivió meses: el click de «Nueva partida» era un `void this.renderWorldSelect()` y sin bridge pulsarlo resultaba un no-op MUDO, y ese silencio era además lo que impedía reproducir #189. El sustituto es `paso(promesa, fuente, qué, alFallar?)` de `nefan-html/src/ui/async-ui.ts`. ALCANCE, dicho para que nadie lo confunda con más de lo que es: el patrón mira UNA LÍNEA — un `void` cuya sentencia empieza y acaba en ella (termina en `;` o en `,`) y que no lleva `.catch` en esa misma línea. Deja fuera a propósito los `void p.catch(...)`, que sí tienen canal, y al `void main()` del GLSL de fps-gl.ts. Su punto ciego es la cadena MULTILÍNEA (`void f()` con el `.then()/.catch()` debajo): reconocerla exigiría emparejar paréntesis, que un regex no hace, y tratarla por línea daba falsos positivos sobre código correcto — la carga diferida de three.js en fps-renderer.ts y el toggle de dev-cache en dev-status-panel.ts, los dos con su `.catch` al final de la cadena. Hoy no queda ninguna cadena multilínea sin canal en el cliente: la única, el `void addTile(scene).then(...)` de main.ts, se arregló en esta misma PR. COMPLEMENTARIA de `@typescript-eslint/no-floating-promises`, activa en `nefan-html/eslint.config.js` desde #248, y NO sustituida por ella: NFP caza al DESCUIDADO —el que llama a una `async function` y sigue, sin `void` ninguno, como hacía `loadSceneFile(value)` en el `change` del selector «Room»— mientras que esta caza al que sí escribe `void` pero se deja el canal, que NFP acepta a propósito (`ignoreVoid: true` es su defecto). Lo que NO ve NINGUNA de las dos son los otros dos puntos ciegos que declara #248: un `void f(); // comentario` (el comentario rompe el ancla de fin de línea `[;,]\\s*$` de este patrón) y un `void f()` en una línea que acaba en `)` —p. ej. dentro de una arrow sin bloque—. NFP no los mira porque LLEVAN `void`. Hoy no tienen ocupante, y siguen sin candado: decirlo aquí es más honesto que dejar creer que entre las dos reglas no cabe nada. MAX: 7. Era 8 —el residuo medido tras dejar a cero los dos ficheros de aquella tanda: fps-atlas 1, dev-menu 1, dev-status-panel 2, graphics-mode 1, history-browser 1, portrait 2— y baja a 7 en la PR de #285: el `void this.show()` de history-browser.ts, que abría el libro hablando con el bridge sin canal para el rechazo, pasó a `paso()` al migrarlo a la puerta de teclado. En title-screen.ts había once: ocho pasaron a `paso()` y tres ni siquiera eran promesas — `renderCharacterEditor`, `renderCreateWorld` y `renderUploadStyle` son síncronos y el `void` no tapaba nada. En main.ts había SIETE, de los que tres eran mudos y se fueron los tres (`addTile` en cadena multilínea, `loadSceneData` y `bootstrap`); los otros cuatro son los `void fpsAtlasController…catch(…)`, que tienen canal y siguen ahí con razón. Misma disciplina que declara `html-sin-catch-silencioso`: cada PR que arregle uno baja el max en el mismo commit.",
      "severity": "warn",
      "max": 7,
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\bvoid\\s+(?!main\\s*\\()[A-Za-z_$](?:(?!\\.catch)[^\\n])*[;,]\\s*$"
      }
    },
    {
      "id": "game-ui-css-sin-literales-de-color",
      "desc": "Ningún literal de color en las reglas de componente de game-ui.css",
      "why": "Todo el color sale de los tokens --nf-* para que el tema de cada style pack funcione. El bloque que DECLARA los tokens sí lleva literales, por definición.",
      "severity": "warn",
      "max": 1,
      "files": [
        "nefan-html/src/ui/game-ui.css"
      ],
      "text": {
        "pattern": "^(?!\\s*--nf-)[^\\n]*:[^\\n]*(?:#[0-9a-fA-F]{3,8}\\b|\\brgba?\\()"
      }
    },
    {
      "id": "solo-el-bridge-normaliza-la-escena",
      "desc": "El cliente 2D no llama a formatDToWorld por su cuenta",
      "why": "El bridge normaliza en el wire. Que el cliente vuelva a normalizar significa que acepta Format D crudo, que es justo lo que prohíbe cliente-no-convierte-celdas-a-metros: dos caminos hasta la world scene divergen. La asimetría se arregla o se documenta, no crece.",
      "severity": "warn",
      "max": 2,
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "formatDToWorld\\s*\\("
      }
    },
    {
      "id": "una-sola-salida-de-escena-del-bridge",
      "desc": "El bridge normaliza la escena para el cliente en un solo sitio: bridge/wire-scene.ts",
      "why": "Una escena sale del bridge hacia un cliente por dos caminos —la que se DIFUNDE (`broadcastScene`) y las que viajan en el `session_started` del resume (`sessionDataForClient`)— y hasta #326 cada uno llamaba a `formatDToWorld` por su cuenta, sincronizados por nada. Lo que le añade trabajo al wire tiene que acordarse de los dos: esta misma tanda le pone encima el combate vivo (la vida que le queda a cada enemigo y la ausencia de los muertos), y con las dos llamadas sueltas el resume habría servido una escena con menos encima que el broadcast — que es la familia exacta de #179, «el resume no re-enriquece». La tercera llamada que apareciera divergiría en silencio y en verde. Hoy las dos pasan por `alWire` en `bridge/wire-scene.ts`, y esta regla es lo que impide que vuelvan a separarse: el tipo no puede decirlo (llamar a una función exportada compila desde cualquier sitio), así que lo dice el checker. Severity error y no `max`: hoy son CERO fuera de sus dos exenciones, y el sitio al que se vuelve no es escribir un normalizador nuevo — es que alguien necesite una world scene «solo para esto» en un handler.",
      "severity": "error",
      "files": [
        "nefan-core/bridge/**/*.ts"
      ],
      "text": {
        "pattern": "formatDToWorld\\s*\\("
      },
      "exceptions": [
        {
          "path": "nefan-core/bridge/wire-scene.ts",
          "reason": "Es LA salida: la única puerta por la que una escena del bridge llega a un cliente, con el combate vivo ya puesto encima."
        },
        {
          "path": "nefan-core/bridge/sim-collision.ts",
          "reason": "No es una salida: normaliza para leer el `terrain_grid` con el que el bridge decide si un NPC puede pisar una celda. Ese resultado no sale al cliente por ningún cable, así que no puede divergir de lo que el jugador ve — lo que se compara con la pantalla es la colisión, y de eso ya hay guiones."
        }
      ]
    },
    {
      "id": "la-atadura-del-save-vive-con-el-dueno-del-mundo",
      "desc": "Solo `bridge/world-claim.ts` ata o suelta el runtime (jugador y combatientes) al save",
      "why": "El sim del bridge es UNO y su combatiente `player` también, así que «quién conduce el sim» y «a qué escucha la partida guardada» son el mismo hecho — y el día que se escribieron como dos llamadas sueltas (`simDriver = ws` por un lado, `bindPlayerRuntime(...)` por otro, movidas a mano desde cuatro sitios) se separaron: la atadura sobrevivió al dueño y el `state.json` de una partida acabó con las coordenadas del muñeco de una fixture dentro, alcanzable por el jugador en dos clicks desde el título (QA 2026-08-25, tres de tres corridas; guion `qa/guiones/25-mirar-fixtures-no-se-lleva-la-partida.mjs`). Un save que se corrompe en silencio es peor que uno que no guarda, porque el jugador se entera al reanudar y no antes. `world-claim.ts` los vuelve a juntar: tomar el mundo y decidir si el save escucha son la MISMA llamada, y soltarlo las deshace las dos. Esta regla es lo que impide que vuelvan a separarse — el tipo no puede hacerlo (TypeScript no sabe decir «este método solo se llama desde este fichero»), así que lo dice el checker. Los tests NO entran en los roots a propósito: `test/narrative-state.test.ts` ata la fuente a mano porque su sujeto es `NarrativeState`, no el bridge, y ahí la atadura no tiene dueño al que sobrevivir. `narrative-state.ts` está exceptuado porque es donde el método se DEFINE. Desde #326 el sujeto son DOS ataduras y no una —el jugador y los demás combatientes, cuya vida es lo que impide que reanudar resucite a los muertos— y las dos entran en el patrón: separarlas sería exactamente el fallo del 2026-08-25 con un sujeto nuevo, una fixture escribiendo la vida de sus muñecos en la partida de quien está jugando. Y el patrón pide el PUNTO, como su regla hermana `la-partida-se-establece-donde-el-jugador-entra`: una llamada de verdad siempre tiene receptor (`narrative.bindCombatantRuntime(...)`), así que la prosa de los comentarios puede seguir nombrando la atadura —`game-loop.ts` explica que su enumerador de combatientes existe para ella— sin poner roja una regla que no ha violado nadie.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts",
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\.bind(Player|Combatant)Runtime\\s*\\("
      },
      "exceptions": [
        {
          "path": "nefan-core/bridge/world-claim.ts",
          "reason": "Es el dueño del mundo: el ÚNICO sitio donde la atadura se pone y se quita, y siempre junto al cambio de dueño."
        },
        {
          "path": "nefan-core/src/narrative/narrative-state.ts",
          "reason": "Es donde se DEFINE el método (y donde vive `save()`, que tira de la fuente). Prohibirlo aquí sería prohibir que exista."
        }
      ]
    },
    {
      "id": "los-plugins-activos-tienen-un-solo-escritor",
      "desc": "Solo `bridge/plugins-activos.ts` reasigna ctx.activePlugins",
      "why": "Tercera de la familia «un solo escritor por hecho» (`la-atadura-del-save-vive-con-el-dueno-del-mundo`, `la-partida-se-establece-donde-el-jugador-entra`), y con el mismo argumento: «qué plugins están activos» es un hecho DE LA SESIÓN — cambia exactamente cuando la sesión cambia (start, resume, la efímera de generate_game) — y hasta el 2026-09-01 lo escribían SEIS asignaciones sueltas en dos handlers, sincronizadas por costumbre. TypeScript no puede cerrarlo (`activePlugins` es un campo público del ctx: asignarlo compila desde cualquier módulo), así que el séptimo escritor —un handler que quisiera «asegurarse» de recargar plugins— entraría en verde y dejaría el registry desalineado del save: los sistemas de una partida juzgando los eventos de otra, sin un solo error visible. Hoy las tres transiciones viven en `bridge/plugins-activos.ts` con su semántica escrita (vaciar ANTES de sembrar para no heredar; activar génesis; atar resume) y esta regla impide que la asignación vuelva a los handlers. ALCANCE exacto, para que nadie lo cite de más: el patrón caza la REASIGNACIÓN (`.activePlugins =`, con el punto como sus hermanas y sin cazar `==`). NO caza (a) la construcción del ctx en `ws-server.ts` y `test/helpers.ts` (`activePlugins: new Map()`, sin punto): es el NACIMIENTO del contexto, no un cambio de sesión, y ahí es donde el campo tiene que inicializarse; (b) la mutación del CONTENIDO del Map vigente — `registerRuntimePlugin` añade entradas vía referencia cuando el motor registra un plugin en runtime, y eso es «un plugin nuevo entra en ESTA sesión», otro hecho con otro dueño (src/plugins/register.ts), que un patrón de texto además no puede seguir a través del alias. Los tests no entran en files, como en las hermanas: fabrican contextos y reasignan como PREMISA del escenario, no como fuga.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts",
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\.activePlugins\\s*=(?!=)"
      },
      "exceptions": [
        {
          "path": "nefan-core/bridge/plugins-activos.ts",
          "reason": "Es el dueño: las tres transiciones de sesión (vaciar, activar génesis, atar resume) viven aquí y en ningún otro sitio."
        }
      ]
    },
    {
      "id": "campos-retirados-no-vuelven",
      "desc": "Un campo de contrato retirado no reaparece en ningún proceso",
      "why": "Pre-producción: el legacy se borra entero el mismo día, no se documenta. El listón es grep a cero. terrain_features volvió a colarse durante meses porque el saneador de ai_server lo inyectaba vacío en cada escena: la deuda se re-creaba sola y cada medida de mutación volvía a contarla. Al retirar un campo, se añade aquí y deja de depender de que alguien se acuerde. Los roots incluyen fixtures (nefan-core/data), tests, narrative-mcp, labs y — desde el 2026-09-01, medido a cero — nefan-core/scripts a propósito: ahí es justo donde un campo retirado reaparece por copy-paste de un dump viejo, y los scripts son además quienes ESCRIBEN dumps y snapshots. Los cuatro términos de la escena SUELTA (issue #172) entran aquí el mismo día que se retira la variante: frontier_request y player_crossed_frontier eran su camino de expansión de mundo, robledo_village y tavern_clearing sus fixtures. qa/**/*.mjs entra en los roots porque los guiones de QA cargan fixtures por nombre. El plató proscenio entra aquí el día que se retira, y con él muere `blueprint-no-importa-stage`: aquella regla prohibía a src/scene/blueprint/** importar de src/scene/stage/**, porque ese único arco (fps-ambience.ts sacaba la hora del día y la huella de los volúmenes del builder del proscenio) mantenía vivas sus 1.467 líneas y habría arrastrado a la fps al borrarlas. Ya no tiene sujeto —el directorio no existe— y una regla que no puede saltar contra nada real es prosa disfrazada de candado; lo que sí puede volver por copy-paste de un dump viejo son sus CAMPOS, y de eso se ocupa este patrón. `stage_request` era la petición de plató en el world_state del motor y `stage_review` su kind de visión; medidos a cero en todos los procesos el día de la retirada. `ortho_shear` era el `kind` de la cámara que el builder del tile emitía para la oblicua (cámara ortográfica + cizalla) y `px_per_cell` la densidad de su canvas: los dos murieron con la vista, y su sitio natural para reaparecer es un dump viejo de labs/fps copiado a mano en vez de regenerado con dump_spec.ts. Los siete términos del pipeline de imagen de la OBLICUA entran el día que se retiran sus huérfanos: `blueprint_review`, `scene_classify` e `image_review` eran kinds de visión del MCP que llevaban meses sin emisor (sus validadores, prompts y fixtures pasaban en CI sobre un camino que nadie recorría desde que #190 se llevó el cliente); `scene_analysis` e `image_elements` eran los dos campos de LlmContext que alimentaba el análisis por visión del tile, y como setTileAnalysis solo lo llamaban sus tests, viajaban SIEMPRE vacíos; `analyze_scene_image` y `generate_scene_image` eran sus rutas HTTP. Un kind y un campo de contexto son justo lo que reaparece copiando a mano un narrative_listen viejo. NO entra aquí `map_plan_reviewed` (sigue vivo en labs/render/runs/_cache/save_scifi.json, un dump de bench que no es mío borrar: el gate nacería rojo). `inpaint_scene_plate`/`peel_scene_layer` estaban exceptuados porque los nombraba el comentario de test_gpu_worker_app.py; ese fichero murió con el gpu-worker (#199), así que hoy su único sitio en el repo es docs/agents (fuera de los roots) y ya NADA impide meterlos en el patrón — queda anotado como backlog, no se hace de paso en esta tanda. Los nueve símbolos del EJE DE VISTAS entran el día que se colapsa: `WORLD_VIEWS`/`WorldView` eran el enum, `WORLD_BRANCHES`/`WorldBranch`/`branchForView`/`VIEWS_BY_BRANCH` las dos fuentes duplicadas de la misma relación vista→rama de contenido, `styleViews`/`viewForRefFile` derivaban qué vistas servía un pack y `ANGLE_BY_VIEW` mapeaba vista→ángulo de sprite. Son símbolos, no campos, y en TypeScript volver a escribirlos ya no compila — pero la mitad de los roots de esta regla NO son TypeScript (fixtures .json, prompts .md, ai_server, labs y los guiones de qa), y ahí un eje resucitado por copy-paste de un dump o de un doc viejo no lo para nadie. El campo de contrato que muere con ellos es `view` (start_session, games_listed, el save y el registro de aplicación de estilo), pero `view` a secas NO puede entrar en el patrón: `derived_views` de plugins y `GET /plugins/{id}/inspect?view=` están vivos y el gate nacería rojo. Las dos CARPETAS de style pack de las vistas retiradas —`overworld` y `proscenium`— entran el día que se archivan sus imágenes: un pack declara sus refs en un .json de nefan-core/data, que es root de esta regla, y el camino por el que vuelven no es escribir código nuevo sino copiar un style.json viejo o un pack de un tercero. En TS el StyleManifestSchema ya las rechaza (folderForRefFile devuelve null y el superRefine falla), pero ai_server NO valida el manifest: su ref_folder devolvería \"\" y la ref se caería del catálogo en silencio, que es justo el fallo caro. Por eso el gate cubre los cinco procesos y no solo el zod. Los TRES alias de sala entran con la tanda #175: `room_id` y `room_description` eran los nombres viejos de `scene_id`/`scene_description`, y `formatDToWorld` los emitía DUPLICADOS en cada world scene — el mismo valor viajando con dos nombres, y el saneador de ai_server reescribiendo el uno desde el otro. Un campo que se copia a sí mismo no es compatibilidad: es un segundo nombre que alguien acabará leyendo, y el día que uno de los dos se retire deja de ser inocuo (bridge/context.ts leía `room_description ?? scene_description`, o sea el legacy PRIMERO). `style_tag` era el tema de escena que elegía la lámina del repintado del tile: murió con la vista oblicua y ai_server ya lo descartaba, pero el world.md de colonia_aster seguía PIDIÉNDOSELO al motor — una instrucción viva a un campo muerto, que es justo por donde un campo retirado se re-genera solo. NO entran en el patrón `room_data`, que es el nombre del sobre del wire MCP (narrative-mcp/server.ts, ai_server/llm_client.py) y está vivo, ni `roomId` en camelCase, que es el mensaje `load_room` del protocolo. Los dos exceptuados de abajo son artefactos GENERADOS y gitignorados: CI no los descarga, así que ahí la regla no canda nada — lo único que conseguiría es poner rojo un árbol local y empujar a borrar un artefacto que costó créditos. OJO a cómo exime el motor de reglas: `checkArchitecture` filtra el FICHERO entero (`!matchesAny(f.path, exceptions)`), no el término, así que esos dos árboles quedan ciegos a los TREINTA términos de este patrón, no solo a los tres que motivaron la exención. Es tolerable porque son artefactos generados y gitignorados —lo que reaparezca ahí lo borra la siguiente regeneración, y CI no los ve nunca—, pero queda escrito para que nadie lo descubra creyendo que ha encontrado un agujero: no lo es, es el precio de eximir por ruta. · isometric_30/isometric_45 (2026-08-24): nombres de la vista oblicua, retirada en agosto de 2026 con todo su código. Sobrevivieron escondidos en dos sitios que nadie mira: el valor POR DEFECTO del `angle` de /skin_sprite_sheet y una tabla de fragmentos de prompt del skinning que era un espejo no declarado de la tabla de ángulos. El efecto era que una petición sin `angle` pedía una vista que ya no existe y moría en un 404 tras cruzar medio sistema. Entran en el patrón porque es por el DEFAULT por donde vuelve un término retirado: nadie lo escribe, se hereda. Hoy `angle` es obligatorio y se valida contra el catálogo del servicio de sprites, que lo deriva de los assets que tiene en disco. · texture_hash/model_hash/texture_prompt/model_prompt (2026-08-24, #199): la cadena de REUSE de assets del gpu-worker. Los dos `*_prompt` no los leía NADIE —su único sitio en el repo era scene_instructions.md: se le pedían al motor y morían ahí, prompt pagado por una capacidad inexistente— y los dos `*_hash` viajaban del tool JSON al zod, al saneador Python y a formatDToWorld para llegar a una world scene que el cliente no lee desde julio. Retirado el worker, ningún proceso puede ya SATISFACER un hash que el contrato exija: dejarlos era exactamente el residuo que produjo este issue. El reuso que sobrevive es el de superficies, y NO es por hash sino por descripción verbatim (cache-hit por prompt), así que no hay campo que reintroducir. Su vía de vuelta es copiar un generate_scene viejo o un dump de entities anterior a la retirada. · save_session/session_saved/SaveSessionMessage/SessionSavedMessage/handleSaveSession/saveSession (2026-08-25, #245): el mensaje de «guardar la partida» y su respuesta. Su handler era el ÚNICO sitio del bridge que refrescaba la posición y la vida del jugador antes de escribir el save… y el mensaje no lo mandaba nadie: `narrativeClient.save()` no tenía un solo llamante en el cliente, así que en trece guardados por sesión la posición nunca se escribía y reanudar te devolvía al origen con 100 de vida. Hoy la frescura viaja ATADA al NarrativeState (`bindPlayerRuntime`, que ata `reseedSimForSession` al sembrar el sim), así que no hay un punto de sincronía que llamar ni que olvidar. Su vía de vuelta es exactamente la que lo dejó vivo tanto tiempo: alguien que quiere «guardar explícitamente» y vuelve a inventar el mensaje en vez de guardar. `saveSession` en camelCase entra porque era el método del bridge-client, y el gate cubre TS de core, bridge, cliente, tests, labs y los guiones de qa. · bootstrap_retry (2026-08-26, #279): la key de cola del REINTENTO del bootstrap que disparaba `resume_session` sobre una sesión sin ninguna escena. Se quedó sin sujeto el día que una partida dejó de existir en disco hasta que el jugador entra en ella: ya no nacen saves de cero escenas, así que ninguna partida en disco puede necesitar el reintento. Entra en el patrón porque su vía de vuelta es un copy-paste de trece líneas —el bloque entero cabía en un `if` al final de handleResumeSession— y porque volvería MUDO: re-encolar un bootstrap sobre una partida que ya tiene mundo no rompe nada visible, solo paga otro turno de motor. La renuncia se tomó a propósito con el coste delante (un bootstrap interrumpido se repaga entero: minutos de motor, cero créditos de imagen), y con ella se fue el «reanudar ES el reintento» de `1dc55ff`. · scattered (2026-08-26, #243): la marca con la que la ruta B del scatter de vegetación etiquetaba las entities `tree` de 1×1 que estampaba en el grid — cientos por tile, PINTADAS y atravesables, mientras la misma zona derivaba aparte sus volúmenes de verdad. El compositor las saltaba por esta bandera y por un backstop de id (`/_z\\\\d+_\\\\d+$/`) «para saves ya guardados»: dos formas de ignorar lo que no debería existir. Retirada la ruta, el campo no tiene emisor ni lector, y su vía de vuelta es exactamente la que lo mantuvo vivo: copiar un dump de entities anterior a la retirada, o un saneador que lo reinyecte. Los snapshots de mundo pre-generados están exceptuados más abajo y los limpia el script de migración de la tanda. · structures (2026-08-30, #301): la primitiva de SALAS del Format D — un rect que el expander estampaba como perímetro de muro cerrado, suelo interior y huecos de puerta, con su propio vocabulario de puertas (`side`/`width`) y sus chars propios (`wall_char`/`floor_char`). Nunca estuvo en `generate_scene.json`, así que el motor no podía emitirla: sobrevivía por el `.passthrough()` de la escena, alimentando 331 líneas de expander, validador y derive que nadie recorría en partida. Medido antes de retirarla: `deriveVolumesFromSchema` da volúmenes byte a byte idénticos con y sin ella sobre los dos tiles pre-generados, porque los dos declaran ADEMÁS el mismo edificio como `volumes` cutaway. Su sustituto es ese cutaway, que es la única forma viva de declarar un interior. Entra en el patrón porque su vía de vuelta no es escribir código nuevo sino copiar un tile de ejemplo anterior a la retirada: los snapshots locales de `data/games/*/world/` (exceptuados abajo, gitignorados) seguirán llevándola dentro para siempre, y de ahí a un fixture hay un copy-paste. · T4 «lo que ya no emite nadie» (2026-09-02, #343 #344 #257 #368-F6): lo que ningún proceso emitía ya y seguía en pie por el lado que lo recibía. El kind WS `bridge_status` (petición, respuesta, sus tipos y `sendBridgeStatus`: su único emisor, `get_bridge_status`, murió con #256); el campo de manifest de plugin `slice_size_hint` (en el schema, sin una línea que lo aplicara); el `sprite_hash` que el cliente leía de cada spawn y que apuntaba a un kind muerto del asset-store; la familia `asset_index_snapshot` del save con `AssetIndex` («wrapper over cache/manifest.json»: nadie la escribía, los 8 saves llevaban `[]`, y su fichero se archiva en esta misma tanda); y los SIETE kinds del asset-store sin productor —texturas PBR, modelos, skins y sprites del gpu-worker (#199), repintados de la oblicua, recortes SAM2 y un type de renders huérfano— con su mapa de directorios, sus seis `*_cache_dir` de la config, la migración desde manifest.json (`migrateManifest`, `RECOVERY_SUBTYPES`), las tablas del lector de blobs (`KIND_TABLE`, `TEXTURE_MAPS`), la ruta muerta `/cache/check` (`CacheCheckResponse`) y los filenames de sus blobs. Las 16.986 filas se purgaron con `scripts/manifest-solo-surface.ts`, exportadas antes a `archivo/cache/manifest-retirado.json` por #293. Los términos son COMPUESTOS a propósito: `texture|model|skin|sprite|scene|segment|plate` a secas colisionan con `sprite_sheet`, `llm_model`, `entity.sprite` y medio repo, y `roughness`/`billboard` son vocabulario vivo de three.js; `cache/sprites` con el `\\b` envolvente no casa `cache/sprite_sheets`, que está vivo. `scene_render` y `bbox.png` nacen con 0 hits: eran un type que solo existía en la DB y el filename de los recortes SAM2 que nunca tuvo código — entran como guardia de reaparición pura, y se dice para que nadie los tome por medidos. Los nueve filenames van con el punto SIN escapar a propósito: `test/repo-hygiene.test.ts` toma la alternancia como lista de términos literales y construye rutas con ellos, y un `\\.` no puede casarse a sí mismo; el `.` regex casa un carácter cualquiera, que aquí solo afloja lo imperceptible (`albedo_png` también saltaría, y tampoco es de nadie). El literal de error del lector de blobs cambió de «Invalid map type» a «Invalid kind» en la misma tanda: el viejo entra en el patrón porque nombra una tabla de mapas de textura que ya no existe.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts",
        "nefan-core/test/**/*.ts",
        "nefan-core/data/**/*.json",
        "nefan-core/data/**/*.md",
        "nefan-html/src/**/*.ts",
        "ai_server/**/*.py",
        "narrative-mcp/**/*.ts",
        "labs/**/*.ts",
        "labs/**/*.mjs",
        "labs/**/*.json",
        "qa/**/*.mjs"
      ],
      "text": {
        "pattern": "\\b(terrain_features|frontier_request|player_crossed_frontier|robledo_village|tavern_clearing|stage_request|stage_review|ortho_shear|px_per_cell|blueprint_review|scene_classify|image_review|scene_analysis|image_elements|analyze_scene_image|generate_scene_image|WORLD_VIEWS|WorldView|WORLD_BRANCHES|WorldBranch|branchForView|VIEWS_BY_BRANCH|styleViews|viewForRefFile|ANGLE_BY_VIEW|overworld|proscenium|room_id|room_description|style_tag|isometric_30|isometric_45|texture_hash|model_hash|texture_prompt|model_prompt|save_session|session_saved|SaveSessionMessage|SessionSavedMessage|handleSaveSession|saveSession|bootstrap_retry|scattered|structures|bridge_status_request|bridge_status_response|BridgeStatusRequestMsg|BridgeStatusResponseMsg|sendBridgeStatus|slice_size_hint|sprite_hash|spriteHash|texture_cache_dir|model_cache_dir|skin_cache_dir|sprite_cache_dir|scene_cache_dir|segment_cache_dir|cache/textures|cache/models|cache/skins|cache/sprites|cache/scenes|cache/segments|scene_render|RECOVERY_SUBTYPES|TEXTURE_MAPS|KIND_TABLE|manifestJsonPath|migrateManifest|subtype_override|CacheCheckResponse|Invalid map type|model/gltf-binary|albedo.png|normal.png|model.glb|skin.png|sprite.png|scene.png|plate.png|segment.png|bbox.png|asset_index_snapshot|setAssetIndexSnapshot|AssetValidator|validateAssetSnapshot|AssetIndex|AssetIndexFilter)\\b"
      },
      "exceptions": [
        {
          "path": "nefan-core/data/contract/arch-rules.json",
          "reason": "es este mismo fichero: el patrón vive aquí por definición"
        },
        {
          "path": "nefan-core/data/games/*/world/**/*.json",
          "reason": "Artefactos de mundo PRE-GENERADOS: árbol local gitignorado (.gitignore:81 canda `world/` ENTERO) que CI nunca descarga. El snapshot persiste la escena tal y como la emitió el motor del día, así que uno anterior a la retirada de los alias los lleva dentro; regenerarlo cuesta créditos y ningún código lee ya el campo. Una regla no puede candar lo que no está en el repositorio: aquí solo pondría rojo el árbol local. El glob va con `**` a propósito, para casar lo MISMO que el .gitignore: bajo `world/` no solo está `tile.json`, también los registros de aplicación de estilo que escribe styleApplicationPath (`world/styles/{style_id}.json`, src/games/style-application.ts:35). Con un solo nivel, uno de esos registros con un término retirado dentro volvería a poner rojo un árbol local por un artefacto generado — exactamente lo que esta exención existe para evitar."
        },
        {
          "path": "labs/style/runs/**",
          "reason": "runs de bench gitignorados (labs/style/.gitignore): material de sesión que no es mío borrar y que CI tampoco ve. Mismo criterio que `map_plan_reviewed` en labs/render/runs — el gate nacería rojo contra un dump histórico que documenta lo que el modelo emitió aquel día."
        },
        {
          "path": "nefan-core/test/architecture.test.ts",
          "reason": "es el arnés de esta regla: para probarla EN NEGATIVO hay que escribir el campo retirado como literal en las fixtures sintéticas. Mismo caso que arch-rules.json — un candado que solo se ha visto verde no es un candado, y la alternativa (concatenar el término en runtime para esquivar el grep) sería exactamente el truco que la regla existe para impedir."
        }
      ]
    },
    {
      "id": "qa-guiones-sin-espera-por-reloj",
      "desc": "Un guion de QA no espera por tiempo de pared: espera por ESTADO",
      "why": "El movimiento va por delta de rAF, el typewriter por setInterval y la generacion por lo que tarde el motor: ningun tiempo de pared es determinista, asi que un guion que duerme es una moneda al aire que depende de lo ocupada que este la maquina. Lo que sustituye al reloj es un REGISTRO que el juego recuerda (ledger de viaje, episodio de tile, corrida de estilo) — el criterio 4 de #210 completo: un aserto fragil no se debilita, se blinda haciendo que el juego recuerde el paso. La regla mira SOLO los guiones: el runner (qa/run.mjs) necesita su polling y qa/lib ofrece las esperas por estado. Y no prohibe `setTimeout` a secas, porque un bucle de MUESTREO por frame (guion 10, opacidades intermedias del velo) es medir estado, no esperar reloj: lo que se prohibe es el sleep — `page.waitForTimeout(...)` y el `new Promise(r => setTimeout(r, N))`. Nace verde el dia que muere `esperarPeticiones` del guion 07, el ultimo que contaba peticiones contra un tope de 90 s. 2026-08-24: el primer guion que necesitó esperar algo FUERA del DOM (un puerto TCP: el guion 20 levanta su propio bridge para provocar el fallo del arranque sin matar el fake-ai-server que comparten los otros 18) llegó con sus dos sondeos escritos a mano y puso esta regla en rojo. La salida NO fue eximir el fichero —cegar un guion entero a una regla `error` sale caro y, sobre todo, callado: el sleep que se cuele mañana ya no lo para nadie—: fue `qa/lib/puertos.mjs`, con `esperarPuertoArriba` (que además falla loud si el proceso se muere, en vez de comerse el cortafuegos) y `esperarPuertoLibre`. Es exactamente lo que este `why` ya preveía al eximir qa/lib, y queda escrito porque el próximo estado que no viva en la pagina tendra la misma respuesta: una espera por condicion en qa/lib, no una excepcion aqui.",
      "severity": "error",
      "files": [
        "qa/guiones/**/*.mjs"
      ],
      "text": {
        "pattern": "waitForTimeout\\s*\\(|new Promise\\s*\\(.{0,40}?setTimeout\\s*\\("
      }
    },
    {
      "id": "la-partida-se-establece-donde-el-jugador-entra",
      "desc": "Solo el handler del ack `session_entered` establece la partida en disco",
      "why": "Desde #279 una partida NACE en `saves/` en un solo sitio: `NarrativeState.establecer()`, llamado desde el handler del ack que el cliente manda cuando el jugador ya se ha vestido Y el mundo está pintado. Antes nacía en `start_session`, antes de generar nada, y un arranque que fallaba después del `ok:true` —el caso medido: un clon limpio sin hojas de sprite— dejaba en el título la tarjeta de una partida que nadie jugó. El tipo ya cierra la puerta de fuera (`SessionStorage` no tiene `write`, así que ningún handler puede CREAR un save por su cuenta), pero no puede cerrar la de dentro: TypeScript no sabe decir «este método público solo se llama desde este fichero», y `establecer()` es público porque el handler vive en otro módulo. Sin esta regla, cualquier handler que quisiera «asegurarse de que la partida se guarda» —el del tile, el del diálogo, el de un sistema nuevo— podría llamarlo y compilaría: la partida volvería a existir antes de que el jugador entrase, y el fallo NO se vería en verde, porque todo lo demás seguiría funcionando. Mismo motivo escrito que `la-atadura-del-save-vive-con-el-dueno-del-mundo`, y misma forma: dueño único, excepciones nombradas. El patrón pide el PUNTO (`\\.establecer\\s*\\(`) a propósito: una llamada de verdad siempre tiene receptor (`ctx.narrative.establecer()`), y así la prosa de los comentarios puede seguir nombrando `establecer()` sin poner roja una regla que no ha violado nadie. `nefan-core/test/**` NO entra en los roots, como en la regla hermana: los tests declaran a mano que una partida existe (es su premisa, no una fuga), y ahí `establecer()` no tiene dueño al que sobrevivir.",
      "severity": "error",
      "files": [
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts",
        "nefan-core/scripts/**/*.ts",
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\.establecer\\s*\\("
      },
      "exceptions": [
        {
          "path": "nefan-core/src/narrative/narrative-state.ts",
          "reason": "Es donde se DEFINE la transición (y donde vive `save()`, la puerta que abre). Prohibirlo aquí sería prohibir que exista."
        },
        {
          "path": "nefan-core/bridge/handlers/session.ts",
          "reason": "Es el dueño del ack: `handleSessionEntered` es el ÚNICO sitio del bridge que traduce «el jugador entró» a «la partida existe»."
        }
      ]
    },
    {
      "id": "un-solo-derivador-del-plan",
      "desc": "El plan del tile lo compone core y viaja resuelto: nadie más lo deriva",
      "why": "Un tile tiene UN plan (ground + volumes declarados + los que el esquema implica) y de él salen cuatro cosas: la geometría 3D, la colisión del jugador, la de los NPCs en el bridge y la máscara con la que el validador juzga la jugabilidad. Hasta 2026-08-26 esa composición estaba copiada cuatro veces —`composeTilePlan` en el cliente, el batch de estilo, la que le FALTABA al bridge (los NPCs atravesaban las casas, #232) y la máscara del validador, que estampaba huellas de entity y no veía un solo árbol derivado (#243)— sincronizadas por un comentario que decía «MISMO plan que compone la partida». No divergían en el código: divergían en los ARGUMENTOS (el seed, qué entra en la máscara), que es como divergen estas cosas de verdad. Hoy `composeTilePlan` (src/scene/tile-plan.ts) es el único compositor y `formatDToWorld` su único llamante en producción; los demás LEEN `__plan`. Esta regla impide que vuelva a haber un segundo derivador: el camino por el que vuelve no es escribir un compositor nuevo, es que alguien necesite un campo del esquema en el cliente y llame a `deriveVolumesFromSchema` «solo para esto».",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts",
        "nefan-core/bridge/**/*.ts"
      ],
      "text": {
        "pattern": "\\b(?:deriveVolumesFromSchema|composeTilePlan)\\b"
      },
      "exceptions": []
    },
    {
      "id": "nadie-inventa-un-puerto",
      "desc": "Los puertos del bloque no se escriben a mano; y un identificador «port» no se inicializa con un literal",
      "why": "LO QUE ESTA REGLA CAZA, exactamente, y nada más:\n  (a) los NUEVE números del bloque que son inequívocamente puertos (9877 9878 8765 8767 8768 8770 3737 18765 9899) escritos EN CUALQUIER SITIO —código, cadena, tabla, comentario—, porque en este repo ninguno de los nueve significa otra cosa (medido: cero ocurrencias legítimas en el ámbito);\n  (b) el 3000, que SÍ es un milisegundo plausible, solo cuando va detrás de `:` o `=` (con comillas opcionales): `port: 3000`, `PORT_HTML=3000`, `PORT_X=\"3000\"`. `HOLD_MS ?? 3000` es legítimo y no se caza a propósito;\n  (c) la FORMA de un puerto aunque el número sea nuevo: un identificador que contiene «port» o «puerto» inicializado con 4-5 cifras, o 4-5 cifras detrás de `127.0.0.1:`, `localhost:`, `ws://…:` o `http://…:`.\n\nLO QUE NO CAZA, dicho para que nadie lo cite como garantía: un puerto NUEVO, fuera del bloque, con un nombre que no contenga «port» ni «puerto» y sin URL delante — `const CTRL = 9955` pasa. No se puede cerrar con un patrón de texto sin cazar toda constante de cuatro cifras del repo, y una regla que cazara `timeout: 30000` enseñaría a añadir excepciones, que es peor. El candado de ESA clase no es un regex más ancho: es que el puerto entre en `CONFIG.ports`, que es lo que se hizo con el 9899 cuando la primera versión de esta regla lo dejó pasar.\n\nPOR QUÉ EXISTE. Los nueve puertos del catálogo estaban declarados CINCO veces —`start.sh` dos veces (las variables y otra vez a mano dentro de SERVICE_LABELS, de donde los leía qa/presets.mjs), `vite.config.ts`, `qa/run.mjs`, `qa/lib/saves.mjs` y los benches de labs— mientras `nefan-core/src/config.ts` decía por escrito, en su cabecera, ser «la fuente única de puertos». El hallazgo no era que faltara una fuente única: era que sobraba todo lo que la ignoraba. Y la copia no era solo duplicación: el guardarraíl de cero créditos reconocía al motor falso por su número de puerto contra una constante que el propio runner había escrito en la URL — una tautología que nunca miró un backend.\n\nTRES VECES SE HA TENIDO QUE APRETAR, y las tres las encontró alguien probándola, no leyéndola: enumerar los nueve dejaba entrar al décimo (`CTRL_PORT ?? 9899`); cazar solo la forma dejaba pasar `const MOTOR_QA = 18765`; y exigir `:`/`=` delante del número dejaba pasar la tabla que esta misma tanda borró, reintroducida palabra por palabra (`[[18765,\"fake-ai-server\"],…]`) y la asignación entrecomillada de bash. Los ficheros de la FUENTE (`src/config.ts`, `src/contracts/service-registry.ts`, el snapshot) no entran en `files`: ahí es donde los números tienen que estar.\n\nY UNA CUARTA VEZ, esta preventiva: `labs/**/*.ts` está junto a `labs/**/*.mjs` porque el motor falso pasó de `.mjs` a `.ts` (#309) y con el glob anterior esta regla habría seguido VERDE sin mirar el fichero — el peor desenlace posible, porque un candado que deja de cubrir a su sujeto se ve exactamente igual que uno que lo cubre.",
      "severity": "error",
      "files": [
        "start.sh",
        "nefan-html/vite.config.ts",
        "qa/**/*.mjs",
        "labs/**/*.mjs",
        "labs/**/*.ts"
      ],
      "text": {
        "pattern": "(\\b[A-Za-z_]*([Pp][Oo][Rr][Tt]|[Pp][Uu][Ee][Rr][Tt][Oo])[A-Za-z_]*\\s*[?:=|]{1,3}\\s*\"?|127\\.0\\.0\\.1:|localhost:|ws://[^ ]*:|http://[^ ]*:)[0-9]{4,5}\\b|\\b(9877|9878|8765|8767|8768|8770|3737|18765|9899)\\b|[:=]\\s*\"?3000\\b"
      }
    },
    {
      "id": "la-fisica-no-se-copia-a-mano",
      "desc": "Los cuerpos del simulador y el mpc del tile no se re-declaran fuera de su fuente",
      "why": "Hermana de `nadie-inventa-un-puerto`, y nace del mismo fallo medido una capa más abajo. #300 topó el `footprint` de una entity móvil con `celdasQueCubreRadio` sobre `NPC_RADIUS_M`/`PLAYER_RADIUS_M`/`TILE_MPC`, y su primera versión resolvió el espejo Python del contrato copiando los tres números a mano en `ai_server/narrative_schemas.py` y derivando el tope de ESA copia. Medido antes de retirarla: movido `NPC_RADIUS_M` solo en TypeScript, el tope TS pasaba a `{npc:3}`, el Python se quedaba en `{npc:2}` y los 136 tests de ai_server seguían en OK; las dos fixtures compartidas tampoco lo cogían, porque la divergencia cae justo en el hueco que no cubren ([8,8] lo rechazan los dos, [2,2] lo aceptan los dos). Un tope declarado en dos sitios que pueden divergir en silencio es, palabra por palabra, el fallo que #300 vino a cerrar. Hoy la vía es la misma que la de los puertos: `scripts/dump-physics.ts` vuelca `data/contract/physics.json` con el tope YA DERIVADO —derivarlo dos veces serían dos fórmulas capaces de divergir— y ai_server lo lee. Lo que esta regla caza es la RE-DECLARACIÓN: cualquiera de los cuatro identificadores asignado a un número fuera de su fuente. Los ficheros de la FUENTE no entran en `files` (src/scene/terrain-collision.ts, src/scene/tile.ts, src/contract/model-io/physics.ts y el script de volcado), porque ahí es donde los números tienen que estar; en TypeScript el resto los importa, así que el ámbito real de esta regla son los procesos que NO pueden importar: ai_server, los guiones de qa y los benches de labs. OJO a lo que NO caza, para que nadie lo cite como garantía: un nombre nuevo (`RADIO_BICHO = 0.7`) pasa, igual que en la regla de los puertos pasa un puerto con nombre inventado. El candado de esa clase no es un regex más ancho —cazar `0.5` cazaría media geometría del repo— sino que el número entre en el snapshot, que es lo que se hizo aquí.",
      "severity": "error",
      "files": [
        "ai_server/**/*.py",
        "qa/**/*.mjs",
        "labs/**/*.mjs",
        "labs/**/*.ts",
        "narrative-mcp/**/*.ts",
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "\\b(NPC_RADIUS_M|PLAYER_RADIUS_M|BODY_RADIUS_M|TILE_MPC)\\s*[:=]\\s*[0-9]"
      }
    },
    {
      "id": "las-respuestas-de-red-no-se-redefinen-en-linea",
      "desc": "El cliente no se inventa la forma de una respuesta HTTP: la importa del contrato",
      "why": "Tercera de la familia de `nadie-inventa-un-puerto` y `la-fisica-no-se-copia-a-mano`: una forma que vive copiada a mano diverge en silencio de su fuente. Medido al entrar (#318): CINCO sitios de nefan-html/src casteaban `(await res.json()) as { … }` con la forma escrita en línea — dos de ellos el MISMO endpoint (`/skin_sprite_sheet`) con formas DISTINTAS, dos leían un `error?: string` que el servicio real no emite jamás con 200 (fail-loud por HTTPException), y dos redactaban `/styles/{id}/complete` con opcionalidad contradictoria entre sí — mientras `StyleCompleteResponse` y `StyleUploadResponse` existían en el contrato sin que nadie los importara ahí. Lo que esta regla caza es el cast a OBJETO ANÓNIMO sobre un `.json()`: `json()) as {` y `json() as {`, con saltos de línea incluidos (el patrón corre con «gm» y `\\s` cruza líneas). Los usos sanos castan a un identificador importado del contrato (`as GenerateSurfaceAtlasResponse`) y no casan con la llave. OJO a lo que NO caza, para que nadie lo cite como garantía: un `interface` local con nombre y su cast (`as RespuestaLocal`), un cast a genérico con objeto dentro (`as Array<{…}>`), y los casts en `nefan-core/test/**`, que queda FUERA a propósito: 14 casts legítimos sobre servidores fake, 3 de ellos el fixture negativo de esta misma regla. T4 (2026-09-02, #344) amplió los roots a `nefan-core/src`, `bridge` y `services` al borrar `AiClient.health()` (casteaba `/health` a `{status?}` sin un solo llamador) y al tipar `prune.ts` con `AssetRefsResponse`, que existía en el contrato sin que nadie lo importara ahí. La garantía total sería de tipo (un fetch tipado por endpoint); esto es el tripwire del modo de fallo observado.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts",
        "nefan-core/src/**/*.ts",
        "nefan-core/bridge/**/*.ts",
        "nefan-core/services/**/*.ts"
      ],
      "text": {
        "pattern": "\\.json\\(\\)\\s*\\)?\\s*as\\s*\\{"
      }
    },
    {
      "id": "solo-se-mata-el-puerto-propio",
      "desc": "Matar por PUERTO (`fuser -k`) o por NOMBRE (`pkill`/`killall`) ocurre en UN solo sitio",
      "why": "LO QUE ESTA REGLA CAZA, exactamente: dos formas de matar que son CIEGAS AL DUEÑO —por PUERTO (`fuser -k`, y también su forma larga `fuser --kill`) y por NOMBRE (`pkill`, `killall`)— y las acota a UNA aparición en todo el ámbito (`start.sh`, `qa/**`, `labs/**`). Esa una es la definición de `kill_port`, el único sitio donde el acto tiene permiso: sus dos llamadas —la tecla `k`/`--parar`, que exige DEMOSTRAR el dueño con `/proc/<pid>` (el cwd bajo $PROJECT_DIR, o unos argumentos que apunten dentro de él: sprite-forge corre desde el repo hermano y su cwd no lo delata), y el `trap EXIT`, que solo recorre STARTED_PORTS— han demostrado el dueño ANTES de llegar. `pkill` entra por su nombre porque es lo que la restricción del usuario prohíbe expresamente («pkill vite / pkill node / pkill python están prohibidos») y no aparecía en ninguna de las 24 reglas.\n\nLO QUE NO CAZA, y es deliberado: matar por PID. `kill -TERM \"$p\"` y `kill -KILL \"$p\"` sobre los PIDs que este proceso arrancó (`cleanup`, `kill_tree`) son el camino CORRECTO —el que sabe de quién es lo que mata— y prohibirlos rompería la bajada. El corolario incómodo, dicho porque QA lo probó: `kill -9 $(pids_del_puerto \"$PORT_HTML\")` —sacar los pids de un puerto y matarlos por PID— PASA esta regla. Es matar a ciegas escrito en dos pasos, y no se puede separar de la bajada legítima con un patrón de texto: las dos terminan en `kill` sobre una variable. Queda fuera y queda escrito.\n\nPOR QUÉ EXISTE. Hasta 2026-08-27 las NUEVE funciones `start_*` de start.sh mataban al ocupante de su puerto antes de arrancar, sin preguntar de quién era: el preset más tonto se llevaba por delante el cliente de otro agente de la máquina, o el narrative-mcp que posee el terminal del motor. Hoy arrancar usa `require_port_free`, que se niega y dice quién ocupa el puerto.\n\n`labs/**/*.ts` entra junto a `labs/**/*.mjs` por lo mismo que en `nadie-inventa-un-puerto`: el motor falso cambió de extensión (#309) y el ámbito tenía que seguirlo.\n\nSE HA TENIDO QUE APRETAR DOS VECES, las dos porque alguien la atacó: la primera versión contaba llamadas al helper `kill_port`, y un `fuser -k` EN LÍNEA dentro de `start_html` —el defecto exacto que este texto describe— salía 44/44 en verde; la segunda cazaba `-k` y no `--kill`. Contar llamadas a un helper no es lo mismo que impedir el acto. Es `warn` y no `error` porque su sujeto es un CONTEO: el acto no está prohibido, está acotado a un sitio.",
      "severity": "warn",
      "max": 1,
      "files": [
        "start.sh",
        "qa/**/*.mjs",
        "labs/**/*.mjs",
        "labs/**/*.ts"
      ],
      "text": {
        "pattern": "fuser\\s+-{1,2}k|\\bpkill\\b|\\bkillall\\b"
      }
    },
    {
      "id": "teclas-de-juego-pasan-por-la-puerta",
      "desc": "El input de juego (keydown, mousedown en window) se registra en un solo sitio",
      "why": "LO QUE CAZA: registrar input de JUEGO por fuera de la puerta — `keydown` (moverse, orientar, elegir ataque, interactuar, respawn, las teclas dev G/B y el libro con H) y `mousedown` (el ataque con LMB). Un solo sitio puede hacerlo: `nefan-html/src/input/puerta-de-teclado.ts`, que descarta mientras `elTituloManda()`.\n\nPOR QUÉ. #246 decidió que el título es un INTERRUPTOR y no una lista de widgets: se escribe `data-titulo` en `<html>` y una regla de `dev-ui.css` apaga `#game-ui` y `#error-log` de golpe, así que un panel nuevo nace ya oculto sin que nadie se acuerde. Esa decisión nunca llegó al INPUT. Medido el 2026-08-28 sobre el árbol limpio: había CUATRO manejadores de `keydown` en `window` (keyboard-input-provider, dev-tools-input, history-browser, dialogue-panel) y ninguno miraba el título, así que con el título delante respondían WASD, las flechas, 1..N, E, R, G, B, H y el clic de ataque. #285 se abrió por UNA de esas teclas (`H` abre el libro dentro de `#game-ui`, que ahí mide cero píxeles: una tecla muda), y gatear solo `H` habría eximido un widget — exactamente lo que #246 rechazó.\n\nALCANCE, y esto es lo importante de leer: **esta regla cubre FORMAS DE ESCRITURA, no el hecho.** Es un patrón sobre el texto, así que es por construcción una lista de sintaxis, y una lista de sintaxis siempre tiene una entrada más. La primera versión pedía `window.addEventListener(\"keydown\"` literal; tres pasadas sucesivas encontraron OCHO formas alcanzables (comillas simples, `document.`, `document.body.`, `globalThis.`, `addEventListener` a secas con el `window` implícito, `window.onkeydown =`, y la forma multilínea que produce prettier), de las que la primera versión solo veía una. Hoy el patrón las acepta todas y su prueba en negativo las enumera, pero no se puede afirmar que sean todas las que hay.\n\nQUIÉN CUBRE EL HECHO: `no-restricted-syntax` en `nefan-html/eslint.config.js`, que mira el AST —el receptor, el nombre del método y el primer argumento— y por tanto no depende de cómo se escriba. Es el candado fuerte; este es el que vive en el contrato del repo, sale en `npm run deuda` y cubre el caso en que alguien desactive o reconfigure eslint. La misma relación COMPLEMENTARIA que declara `html-sin-promesa-muda` con `no-floating-promises`, y por el mismo motivo.\n\nSU PUNTO CIEGO, que tampoco cierra el AST: el receptor por ALIAS (`const w = window; w.addEventListener(\"keydown\", …)`). Reconocerlo exige seguir el tipo de una variable. No tiene ocupante hoy y queda escrito, que es más honesto que dejar creer que entre las formas no cabe nada.\n\nLO QUE NO CAZA, y es deliberado: (1) `keyup`, porque gatear la SOLTADA es peor que no gatear — si el título aparece con `W` pulsada y se descarta su `keyup`, el jugador anda solo para siempre al volver; lo que se descarta es la intención nueva, no el fin de una vieja. (2) `mousemove`, que acumula la mirada y no actúa. (3) Los listeners sobre ELEMENTOS (`this.inputEl.addEventListener` del texto libre del diálogo, los botones del título): no son input de juego, son controles con foco, y el título los necesita vivos — de ahí el `(?<![.\\w])` de la forma a secas, que deja pasar `el.addEventListener`.\n\nEs `error` y no `warn` porque entra a CERO: los cuatro manejadores se migraron en la misma PR.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "(?:window|globalThis|document\\.body|document)\\.addEventListener\\(\\s*[\"'](?:keydown|mousedown)|(?<![.\\w])addEventListener\\(\\s*[\"'](?:keydown|mousedown)|\\bon(?:keydown|mousedown)\\s*="
      },
      "exceptions": [
        {
          "path": "nefan-html/src/input/puerta-de-teclado.ts",
          "reason": "Es LA puerta: aquí se DEFINE el registro que descarta mientras el título manda. Prohibirlo aquí sería prohibir que exista. Su prosa nombra el patrón, y por eso la exención cubre el fichero entero y no una línea."
        }
      ]
    },
    {
      "id": "el-gate-del-dialogo-no-vuelve-a-ser-un-campo",
      "desc": "El proveedor de input PREGUNTA si hay diálogo abierto; no guarda una copia",
      "why": "LO QUE CAZA: que `dialogueActive` reaparezca en `nefan-html/src/input/**` — como campo de `InputProvider`, como propiedad de una implementación, o como prosa que lo dé por vivo.\n\nPOR QUÉ NO BASTA EL TIPO, que es la pregunta honesta que hay que contestar antes de añadir un checker. #314 quitó el campo público: hoy el proveedor recibe `InputDeps.dialogoAbierto()` y ningún módulo de fuera puede escribir el gate, porque no hay nada que escribir. Esa es la garantía fuerte y la da `tsc`. Lo que `tsc` NO impide es re-DECLARARLO: añadir `dialogueActive = false` a `KeyboardInputProvider` compila tan feliz, no rompe ningún test, y a partir de ahí hay otra vez dos representaciones de «hay una conversación abierta» que nadie obliga a coincidir — que es exactamente el estado del que salieron #311 y #314. La regla cubre la REAPARICIÓN, no la escritura; por eso es de la familia de `campos-retirados-no-vuelven` y no un `warn` con tope.\n\nPOR QUÉ AQUÍ Y NO EN UN TEST: `nefan-html` no tiene harness que pueda ponerse rojo (#241). Mientras eso siga así, el candado del cliente es una regla de contrato o no es nada.\n\nALCANCE, dicho para que no se lea de más: solo `nefan-html/src/input/**`, que es el sitio donde el espejo VIVÍA.\n\nESTE PÁRRAFO DECÍA UNA COSA FALSA y se corrige el 2026-09-01 (#358). Decía que `main.ts` quedaba fuera porque el hook de bench expone la clave `dialogueActive` en `__nefan.state()` «y la leen los guiones 37, 41 y 42». No la leían: `grep -rn dialogueActive qa/ labs/` daba CERO lectores de código desde el 2026-08-30 —los 37 y 43 solo la nombran en prosa, contando por qué se quitó el vigilante que la comparaba consigo misma— y los que preguntan por el diálogo usan `puedeAtacar()`. La clave se ha retirado del hook con esta misma tanda, así que hoy el patrón tiene cero ocurrencias en TODO `nefan-html/src`, no solo en `input/**`. El alcance se deja donde estaba a propósito: ampliarlo a `src/**` sería un candado distinto —«este nombre no vuelve a existir en el cliente»— y esta regla existe para otra cosa, para que el ESPEJO no renazca donde vivía. Que hoy no haya ocupantes fuera de su alcance es un hecho, no una razón para ensancharla sin decidirlo.\n\nEs `error` y entra a CERO ocupantes y CERO exenciones: se midió antes de ponerla (`npm run deuda` = 66 items · Fronteras 15, idéntico con y sin ella). Un candado que hay que pagar para meter no entra.",
      "severity": "error",
      "files": [
        "nefan-html/src/input/**/*.ts"
      ],
      "text": {
        "pattern": "\\bdialogueActive\\b"
      }
    },
    {
      "id": "ningun-aviso-culpa-al-motor",
      "desc": "Un `narrative_status` de error del bridge no sale con `kind: \"consequences\"` salvo donde el motor narrativo SÍ rechazó",
      "why": "LO QUE CAZA: que un aviso a pantalla completa vuelva a salir bajo «El motor narrativo rechazó la respuesta» contando otra cosa.\n\nEL DEFECTO (#352). `kind` es lo único que el jugador lee como TITULAR, y hasta el 2026-09-01 el bridge solo tenía tres kinds: `scene`, `tile` y `consequences`. Todo lo que no era generación de mundo salía por el tercero, y `rotuloDeStatus` lo cerraba con un `return` catch-all. Resultado medido: SIETE emisores bajo el mismo titular y SEIS mintiendo — el «no pude restaurar esto del save» del resume (handlers/session.ts), el takeover de sesión y los dos «no se pudo guardar» (handlers/dialogue.ts, handlers/simulation.ts), el plugin que revienta su turno (context.ts) y el handler que revienta (router.ts). El cuerpo de cada uno era exacto y estaba en idioma de jugador; lo que mentía era el rótulo, y encima nombraba a un culpable concreto que no había intervenido.\n\nPOR QUÉ NO BASTA EL TIPO, que es la pregunta que hay que contestar antes de meter un checker. El `switch` exhaustivo con `const nunca: never` de `rotuloDeStatus` canda la MITAD que le toca: un kind nuevo sin titular propio no compila. Lo que `tsc` no puede decir es que un emisor NUEVO no elija `consequences` para contar algo que no es un rechazo del motor — escribir `kind: \"consequences\"` compila desde cualquier handler, es exactamente cómo llegaron aquí los seis, y es gratis (el kind ya existe y el rótulo ya está escrito). El tipo canda el rótulo; el checker canda la ATRIBUCIÓN.\n\nLÍMITE HONESTO, para que nadie lo lea de más: la regla exime el FICHERO, así que no atrapa a un emisor nuevo escrito DENTRO de `handlers/dialogue.ts` — que es el único sitio donde `consequences` es cierto y por eso el único exceptuado. Para eso está el `switch`, y para eso está que el fichero exceptuado sea el más pequeño posible. Tampoco mira `phase`: un `ready` con ese kind también salta, y es a propósito — el kind describe QUÉ ha pasado, no si ha ido bien (el aviso de plugin activado de `ws-server.ts` era `ready/consequences` y se pasó a `plugin` en esta misma tanda; no rotulaba nunca, pero era el décimo sitio donde `consequences` significaba «lo demás»).\n\nEs `error` y entra a CERO ocupantes fuera de su única excepción: medido con `npm run deuda` antes y después (69 items, Fronteras 15, idéntico). Un candado que hay que pagar para meter no entra.",
      "severity": "error",
      "files": [
        "nefan-core/bridge/**/*.ts"
      ],
      "text": {
        "pattern": "kind:\\s*\"consequences\""
      },
      "exceptions": [
        {
          "path": "nefan-core/bridge/handlers/dialogue.ts",
          "reason": "Es el ÚNICO rechazo real del motor narrativo: `reportPlayerChoice` devuelve `ok:false` y lo que ha fallado es, literalmente, la respuesta del motor a la elección del jugador. Los otros dos avisos de este mismo fichero —el takeover y el «no se pudo guardar»— NO están exceptuados por serlo: se les cambió el kind en la misma tanda (`takeover` y `save`), así que la exención cubre el fichero pero dentro de él solo queda un ocupante."
        }
      ]
    },
    {
      "id": "el-mundo-del-cliente-tiene-un-solo-dueño",
      "desc": "Los cuerpos del mundo y la mirada del jugador no vuelven a ser `let` de módulo en el cliente",
      "why": "LO QUE CAZA: que `npcEntities`, `objectEntities`, `enemyEntities`, `activeTileKey`, `sceneData`, `currentExits`, `playerYaw`, `playerPitch` o `playerForward` vuelvan a nacer como `let` a nivel de módulo en cualquier fichero de `nefan-html/src`.\n\nPOR QUÉ NO BASTA EL TIPO, que es la pregunta honesta antes de añadir un checker. Hoy ese estado son campos `#privados` de `MundoDelCliente` (`world/mundo-del-cliente.ts`) y de `Mirada` (`nefan-core/src/simulation/mirada.ts`): no queda binding exportado que copiar, escribirlo desde fuera es imposible, y una copia huérfana que alguien deje atrás la caza `@typescript-eslint/no-unused-vars`, que ya es `error` en el paquete. Esa es la garantía fuerte. Lo que NADIE impide es RE-DECLARARLO: escribir otra vez `let npcEntities: Entity[] = []` en un módulo nuevo compila tan feliz, y a partir de ahí hay dos mundos que nadie obliga a coincidir. La regla cubre la REAPARICIÓN, no la escritura; misma familia que `campos-retirados-no-vuelven` y que `el-gate-del-dialogo-no-vuelve-a-ser-un-campo`.\n\nEL RIESGO QUE MOTIVA ESTO, medido durante el troceo de #358: al partir `main.ts` (3.136 líneas, 40 `let` de módulo) el peligro NO es escribir desde otro módulo una `let` importada —eso es `TS2632` y lo caza `tsc --noEmit`, que corre en CI—, es DUPLICARLA en vez de moverla: compila limpio, pasa lint, y deja dos copias con la mitad de los escritores cada una. El compilador enumeró las 60 referencias en cuanto la `let` desapareció; lo que no puede enumerar es la copia que alguien cree nueva.\n\nPOR QUÉ AQUÍ Y NO EN UN TEST: `nefan-html` no tiene harness que pueda ponerse rojo (#241). Mientras eso siga así, el candado del cliente es una regla de contrato o no es nada.\n\nQUÉ FORMAS CAZA, porque la primera versión de esta regla prometía «en cualquier fichero» y entregaba menos (QA, 2026-09-01): `let X`, `var X` y `export let X`, a CUALQUIER sangría. El patrón original era `^let ` y se le escapaban las tres — y la que más importa es `export let`, que es justo la forma que tomaría una copia pensada para leerse desde otro módulo, o sea el escenario que este mismo `why` describe como el riesgo real del troceo.\n\nLA SANGRÍA ENTRA A PROPÓSITO, y no es un descuido de precisión: un `let npcEntities` dentro de una función tampoco es inocente. O es una copia del mundo, o es una sombra del nombre del dueño; las dos son la confusión que la regla existe para impedir, y con estos nueve nombres no hay un tercer motivo para escribirlo.\n\nLO QUE NO CAZA, dicho para que no se lea de más:\n\n  · `const`, a propósito. `const npcs = mundo.npcs` es un alias LEGÍTIMO y frecuente: apunta a la lista del dueño, no la duplica. Lo peligroso es un segundo binding MUTABLE, que es lo que declaran `let` y `var`.\n  · una `let` con otro nombre (`worldNpcs`, `misNpcs`). El patrón es una lista de NOMBRES, no una idea, igual que `campos-retirados-no-vuelven`. Contra eso lo que hay es el tipo: mientras el dueño sea el único que tiene los cuerpos, una lista paralela no se llena sola.\n\nEs `error` y entra a CERO ocupantes y CERO exenciones: se midió antes de ponerla (`npm run deuda` = 71 items · Fronteras 15, idéntico con y sin ella). Un candado que hay que pagar para meter no entra.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "^\\s*(?:export\\s+)?(?:let|var)\\s+(npcEntities|objectEntities|enemyEntities|activeTileKey|sceneData|currentExits|playerYaw|playerPitch|playerForward)\\b"
      }
    },
    {
      "id": "el-tope-de-tamano-no-se-apaga-con-un-comentario",
      "desc": "Ningún fichero del cliente se saca del régimen de eslint con un `eslint-disable` de fichero",
      "why": "LO QUE CAZA: un `eslint-disable` de FICHERO —`/* eslint-disable */`, `/* eslint-disable max-lines */`, `/* eslint-disable max-lines, no-console */`— en cualquier `.ts` de `nefan-html/src`.\n\nPOR QUÉ EXISTE, y por qué en OTRO motor que el candado al que protege. El tope de tamaño del cliente (`max-lines` en `nefan-html/eslint.config.js`, #358) es lo que impide que `main.ts` recupere mañana las líneas que perdió hoy. Medido por QA el 2026-09-01: un fichero de 600 líneas cuya PRIMERA LÍNEA sea `/* eslint-disable max-lines */` pasa `npx eslint` con exit 0 y sin una palabra en la salida. O sea que el candado que la tanda vino a poner se apaga con un comentario escrito por la misma persona que está a punto de pasarse del tope.\n\nY eso no es «una regla más que se puede desactivar»: el problema que abre #358 es literalmente que el tamaño era «el único invariante estructural que depende de que alguien se acuerde». Un tope que se apaga con una línea no cambia esa naturaleza, solo mueve el sitio donde hay que acordarse — y es un rodeo MÁS BARATO que el `main-2.ts` que el usuario cerró eligiendo el tope global en vez de uno de tres nombres.\n\nUn checker de eslint no puede cerrarlo (el disable lo desactivaría a él también, o a la regla que lo intentara). Por eso vive aquí: `checkArchitecture` corre en `npm test`, no lee comentarios de configuración de eslint y no hay directiva que lo silencie. Es la misma relación COMPLEMENTARIA que ya declaran `teclas-de-juego-pasan-por-la-puerta` y su gemela de `no-restricted-syntax`, con los papeles cambiados: allí eslint es el candado fuerte y esto el de respaldo; aquí eslint CUENTA las líneas y esto impide que lo callen.\n\nALCANCE: prohíbe la forma de FICHERO y NINGUNA otra. `// eslint-disable-next-line <regla>` y `/* eslint-disable-line */` siguen permitidos, que son el escape local, visible y acotado de siempre. La distinción no es de gusto: `max-lines` se reporta en la ÚLTIMA línea del fichero, así que la forma de línea NO puede silenciarlo (medido: con `eslint-disable-next-line max-lines` el error salta igual, y encima la directiva sale marcada como no usada). La de fichero es la única que lo apaga, y por eso es la única prohibida.\n\nES MÁS ANCHO QUE `max-lines` A PROPÓSITO, y conviene decirlo en vez de fingir precisión: prohíbe el disable de fichero para CUALQUIER regla. Acotarlo a `max-lines` dejaría abierto el `/* eslint-disable */` pelado —que también la apaga— y obligaría a un patrón con retroceso, que es justo el coste que hizo que este candado viviera en eslint y no aquí. Sacar un fichero entero del régimen de lint es, además, la clase de decisión que merece una excepción declarada en el contrato y no un comentario.\n\nEs `error` y entra a CERO ocupantes y CERO exenciones: `grep -rn eslint-disable nefan-html/src` = 0.",
      "severity": "error",
      "files": [
        "nefan-html/src/**/*.ts"
      ],
      "text": {
        "pattern": "/\\*\\s*eslint-disable(?![-\\w])"
      }
    }
  ]
}
