{
  "name": "verify_weapon",
  "description": "Check that a weapon is correctly placed in a character's hand for combat stance, and suggest a rotation correction if not.",
  "input_schema": {
    "type": "object",
    "properties": {
      "ok": {
        "type": "boolean",
        "description": "true si el arma está bien colocada en la mano para combate"
      },
      "issue": {
        "type": "string",
        "description": "Qué está mal (cuando ok=false)"
      },
      "suggested_delta_euler": {
        "type": "array",
        "items": [
          {
            "type": "number"
          },
          {
            "type": "number"
          },
          {
            "type": "number"
          }
        ],
        "minItems": 3,
        "maxItems": 3,
        "description": "Corrección sugerida [rx, ry, rz] en grados (cuando ok=false)"
      }
    },
    "required": [
      "ok"
    ]
  }
}
