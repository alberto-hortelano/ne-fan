# Crítica · Tanda B «lo que el motor manda»

**#532 REENCUADRADA · #524 VIGENTE · #529 REENCUADRADA.** Nada que descartar; dos encuadres que corregir antes de que el arquitecto elija por moneda.

**El problema real, una frase por pieza.** #532: el motor dice QUÉ pone y no puede decir cuánto ocupa ni si se pisa, así que lo decide el juego con un número. #524: el reparto de un turno separa por una constante que no sabe el tamaño de lo que separa. #529: un solo criterio de «enemigo utilizable» con dos desenlaces, y manda el destructivo.

## La premisa, afirmación por afirmación

**«Todo `object` del motor es un muro sólido de 1,5 m» — CIERTO, camino completo.** El motor solo puede declarar `{type, entity_kind: npc|building|object, name, description?, position_hint?, role?, style_ref?}` (`nefan-core/src/contract/model-io/schemas.ts:38-66`) → el bridge deriva `sizeXZ: huellaEnMetros(kind)` (`src/narrative/consequence-handler.ts:149`) sobre `FOOTPRINT_POR_DEFECTO.object = [3,3]` celdas (`src/scene/scene-normalize.ts:177-180`) × `TILE_MPC = 0.5` (`src/scene/tile.ts:16`) = **1,5 × 1,5 m** → el cliente lo monta con `category:"prop"` y `dueno:{de:"runtime"}` (`nefan-html/src/world/materializar-spawn.ts:136-160`) → `aabbBloquea` bloquea: tiene `sizeXZ`, la categoría está en la lista y por ser de runtime nunca se salta (`nefan-core/src/simulation/obstaculos-del-jugador.ts:112-115`).

**«Toca zod, espejo Python y tool JSON» — INCOMPLETO.** El tool JSON y el prompt NO se tocan a mano: los genera `scripts/gen-contract.ts` desde el zod, con candado en `test/contract-model-io.test.ts`. Los sitios reales son siete, y tres no están en `requisitos.md`:

1. `src/contract/model-io/schemas.ts` (zod SoT) → codegen a `data/contract/tools/narrative_react.json` y `prompts/narrative_event.md`.
2. `ai_server/narrative_schemas.py`: `valid_kinds` (:1080) **y la allow-list** (:1144-1170). **MEDIDO HOY**: `validate_narrative_reaction({entity_kind:"object", name:"Carro", footprint:[6,6]})` devuelve el objeto **sin `footprint`**. Llegaría vivo de contrato y muerto de datos, como ya pasó con `role`/`style_ref`.
3. `src/narrative/types.ts:139-158` (`Consequence`) y `:353-367` (`HuellaDelSpawn`, unión cerrada).
4. `consequence-handler.ts:149` + `scene-normalize.ts:188-206` (`huellaEnMetros`, fail-loud por kind).
5. **`src/session/mundo-persistido.ts:447` y `:517` — el RESUME, que nadie nombró.** `CLASES_QUE_VUELVEN` no tiene `item` (un ítem reanudado saldría con «el juego no sabe pintar nada de tipo "item"») y la huella se re-deriva de `rec.type` ignorando lo declarado: un carro `[6,6]` vuelve de 3×3. El `footprint` SÍ está en el save (`data` es la consequence entera), pero nadie lo lee.
6. `materializar-spawn.ts:136-160`: hoy el kind es un booleano (`isBuilding`).
7. **Rastro**: `data/contract/prompts/ui_systems.md:56` («Their footprint blocks movement either way»), la cabecera de `mundo-persistido.ts:432` («la huella no está en el save y no hace falta que esté») y la del guion 91, que afirma lo mismo.

**Y los tres candados no cubren lo que importa**: `contract-fixtures.test.ts` y `ai_server/tests/test_contract_fixtures.py` comparan **accept/reject**, no si el campo sobrevive. Un `footprint` en el zod y no en la allow-list pasa las dos suites en verde y no llega nunca.

**¿(a) y (b) son redundantes? NO — y no por lo que dice el issue.** `aabbBloquea` filtra por **categoría** (`building|prop`), no por tamaño: `kind:"item"` —que ya existe entero en el camino de la escena (`scene-normalize.ts:324`, color `#aa8` en `carga-de-tile.ts:103`, `KIND_DEFAULT_HEIGHT.item = 0.5`)— es lo único que hace que algo se pise, y sale casi gratis; `footprint` solo dice cuánto ocupa. (a) sola **no** cumple el criterio 1: una bolsa con `footprint:[1,1]` seguiría siendo una caja sólida de 0,5 m. **Pero el cuerpo de #532 se contradice al sumarlas**: su (a) dice «`object` sin footprint deja de ser sólido» y su (b) «`object` conserva la huella 3×3». Sumadas tal cual, el carro sin `footprint` deja de frenar y se incumple el criterio 1. Con (c) elegida, la mitad viva es la de (b): **`object` sigue sólido con 3×3 por defecto, `item` es la clase que se pisa, `footprint` solo afina.** Que quede escrito, o se decide en el plan sin que nadie lo vea.

**#524: los 1,8 m — CIERTO.** `SEPARACION_M = 1.8` en `consequence-handler.ts:186`, lo aplica `resolvePositionHint` (`:192-217`) en el bridge, alternando ±1,8 m perpendicular al forward y contando **todos** los spawns del turno sea cual sea su clase. Con `PLAYER_RADIUS_M = 0.4` (`src/scene/terrain-collision.ts:23`): dos `object` dejan 0,3 m entre caras (intransitable); dos `building` de 4 m **se solapan 0,4 m**; un `npc` junto a un `building` del mismo turno cae **dentro** de su caja (1,8 < 2 m de media huella). El guion 91 ya lo mide y lo dice en su cabecera. Matiz: «el jugador queda encajonado» es la mitad débil — `aabbBloquea` tiene «salir sí, entrar no» (`:118-120`), así que al solapado no se le atrapa; lo demostrable es que las cajas se solapan entre ellas y con los NPCs. **«Hacerlo antes es escribir un test que #532 reescribe» — CIERTO en lo sustancial**: #532 cambia la ENTRADA de la función (huella declarada, más una clase que no ocupa sitio), así que no se rehace el test sino la aritmética. Al revés no: nada de #532 necesita #524.

**#529: las dos conductas conviven — CIERTO.** Bridge: `nefan-core/bridge/ws-server.ts:286-309` — si `intakeClientMessage` falla, unicast `narrative_status{kind:"protocolo"}` y `return`; **el frame entero se descarta**, y `src/protocol/status-rotulo.ts:205` lo convierte en `{destino:"overlay", titulo:"Fallo interno del juego"}`. Cliente: `nefan-html/src/scene/enemigo.ts:62-68` — `errors.push("scene", 'enemigo "…" descartado: …')`, `return null`, y el llamante sigue con los demás.

**«¿Un criterio o dos?» — UNO, y ya candado.** `parseHostileCombat` (`src/combat/hostil-desde-combat.ts:144`) lo aplican las dos puertas (`src/protocol/message-schema.ts:79`, `enemigo.ts:64`) y el resume (`mundo-persistido.ts:186`); lo sujetan la regla `la-logica-de-juego-no-vuelve-al-cliente` con negativo escrito (`test/architecture.test.ts:1174-1232`) y el guion 90. **#530 está cerrada** (`combat_range` obligatorio, `src/types.ts:86`; los cuatro campos cruzan el zod con `z.custom` para que el motivo lo dé siempre el parser). **El criterio 4 de `requisitos.md` ya se cumple hoy**: no es trabajo, es regresión a vigilar.

**Dos premisas de #529 que el código desmiente.** (i) «el motor narrativo produce estos bloques» es **falso**: el motor declara `role:"hostile"` y los números los deriva el core (`src/combat/hostiles.ts:63-79`, «ÚNICA fuente del bloque `combat`»). (ii) «el motivo vuelve al motor» no tiene destinatario en ese borde: al otro lado del WS está el cliente, no el modelo, y el fail-loud-al-modelo vive en el pre-flight MCP. Consecuencia: con el gate del cliente por delante, **hoy ningún camino de jugador llega a ese modal** — QA lo alcanzó inyectando frames por el socket del juego. El valor de #529 es coherencia y defensa en profundidad, no dolor medido.

## El día después

Se gana: el motor puede poner una llave que se pisa y un carro que se rodea, y lo que pone deja de mentir al reanudar. Se cierra una puerta: `HuellaDelSpawn` deja de ser una unión de dos ramas limpias, y quien mañana quiera «sólido» independiente del kind se encontrará tres diales donde hoy hay uno. Hay que **borrar** el rastro del punto 7 — es lo que esta casa ya se ha cobrado dos veces. Nada queda para tirar: esto solo añade. Parecerá arbitrario en un mes que el spawn diga `object` donde la escena dice `prop`; no se arregla aquí, pero que el plan lo mencione.

**La distinción `dueno` no se toca**: `item` no bloquea por la LISTA de categorías, no por el dueño, y `footprint` solo cambia el tamaño. Matiz al aviso de `requisitos.md`: no es que «solo los spawns de runtime son sólidos» — la caja del tile sigue siendo su red de seguridad mientras su plan no esté instalado (`obstaculos-del-jugador.ts:114`). Y el candado `cliente-no-convierte-celdas-a-metros` obliga a que `footprint` viaje en **celdas** y lo convierta core: pasarlo crudo al cliente nace rojo.

## Conflictos

- Ninguna pieza choca con otra decisión viva; #377 y #325 quedan fuera por etiqueta, correctamente.
- **Tanda C**: sin colisión de edición en `nefan-html/src/ui/etiquetas-del-mundo.ts`, pero sí de conducta — su `:124` calcula el radio de mirilla desde `e.sizeXZ`, y B cambia qué `sizeXZ` trae un spawn. **Orden: B antes que C**, o la QA visual de C mide sobre datos que van a cambiar. En `nefan-html/src/main.ts` sí hay colisión de fichero (B en el rótulo de status `:1052`; C en el chip y el muro `:547`/`:1178`): zonas distintas, conflicto de merge, no de diseño.
- **Tanda A**: sin solape (`ui/titulo/`, `session/gates-de-imagen.ts`).
- **Vecinos sin tanda**: **#490** vive en `loadSession`/`spawnsDeRuntime`, justo donde #532 obliga a entrar. **#509/#497** son el registro de errores donde #529 escribirá más líneas. Decidid si entran; no lo descubráis a mitad.
- **Barato**: #532 pide «guion 91» y el 91 ya existe (es el de #489). El siguiente libre es el 100.

## Coste contra valor

#532 es la cara: siete sitios, tres procesos y tres cabeceras que quedan mintiendo. Vale la pena — sin ella el motor no puede poner un objeto pequeño, que es lo que más pone. #524 cuesta poco **detrás de #532 en la misma tanda** y mucho delante. #529 es barato (el mecanismo ya existe: `status-rotulo.ts:139` tiene `destino:"log"`) y su valor es coherencia; si hubiera que recortar la tanda, es la pieza que puede esperar. «No hacer nada» solo es defendible en #529.

## Qué le cambiaría a `requisitos.md` (redactado para pegar)

1. §1, tras «(c) ELEGIDA»: «El cuerpo de #532 se contradice al sumar (a) y (b): la mitad viva es la de (b). `object` **conserva** su huella 3×3 por defecto y sigue frenando; `item` es la clase que no frena; `footprint` (celdas) solo afina el tamaño. Ningún spawn deja de ser sólido por omitir un campo.»
2. Sustituir el aviso de los tres sitios: «El contrato se escribe en UN sitio (el zod de `src/contract/model-io/schemas.ts`); el tool JSON y el prompt los genera `npm run gen:contract`. A mano se tocan el **espejo Python** (`valid_kinds` **y la allow-list de `validate_narrative_reaction`**, que hoy tira en silencio cualquier campo nuevo — medido), el **resume** (`session/mundo-persistido.ts`: `CLASES_QUE_VUELVEN` y la huella re-derivada del `type`), el materializador del cliente y el rastro de prosa (`prompts/ui_systems.md:56`, `mundo-persistido.ts:432`, cabecera del guion 91). Las fixtures compartidas comparan accept/reject, **no** si el campo sobrevive: eso hay que candarlo aparte.»
3. §3, añadir: «El criterio ya es UNO desde #531 y #530 (`parseHostileCombat`, candado en `architecture.test.ts` y guion 90): el **criterio de aceptación 4 ya se cumple** y aquí solo se vigila. Lo que cambia es el DESENLACE.»
4. Reescribir el criterio 3: «Si un frame de `add_combatants`/`load_room` trae un enemigo inválido junto a dos válidos, entran los dos válidos, el malo no, y el motivo llega al log del bridge y al registro del jugador **sin modal**.» Quitando «el motivo llega al motor»: el motor no manda ese frame (los números los deriva `combatForHostileRole`) y ese borde no tiene canal de vuelta al modelo.
5. Avisos: «la caja de lo que declara un tile se aplica mientras su plan no esté instalado; lo de runtime se aplica siempre» (no «solo los spawns de runtime son sólidos»). Y añadir el candado `cliente-no-convierte-celdas-a-metros`: `footprint` viaja en celdas y lo convierte core.
6. Fuera de alcance: «#490 (ids duplicados en el resume) y #509/#497 (el registro de errores) son vecinos de lo que se toca; si no entran, que se diga.»
