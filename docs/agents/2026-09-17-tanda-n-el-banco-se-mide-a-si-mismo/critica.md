# Crítica — Tanda N «El banco se mide a sí mismo»

**#606 · REENCUADRADA** — es **2,5× mayor** (15 copias, no 6) y su peaje medido son **112 s**, no la batería.
**#637 · VIGENTE**, con una corrección: su aserto **no es expresable hoy** y los vecinos ciegos no son 27 y 92, son **51 y 53**.
**#634 · REENCUADRADA** — **no bloqueada**: existe una cuarta salida que el issue no lista y que no toca ningún suelo. La (a) sí está bloqueada, con motivo más fuerte que el escrito.

## El problema real, en una frase

- **#606** — la espera por fotogramas del banco no tiene dueño: el día que cambie su fuente o su cortafuegos hay N sitios que tocar a mano y el que se quede atrás sigue verde. La solución propuesta ataca el problema; lo que falla es el **alcance**.
- **#637** — el 82 **lee** el libro de skins de la partida B y no lo mira: la identidad del aspecto del jugador no la afirma nadie. La solución propuesta ataca el problema.
- **#634** — el 75 es un candado que unas veces mide y otras tira una moneda. Las tres salidas del issue lo atacan **por el lado caro**: dos tocan algo con suelo o con doctrina, y la tercera es rendirse.

## La premisa, afirmación por afirmación

### #606
| Afirmación | Verificación |
|---|---|
| «6 copias de `esperarFrames`» | **Cierto**: 16:42, 22:80, 30:38, 31:70, 44:53, 45:49. Cinco idénticas; la 44 renombra el parámetro interno |
| «22 llamadas» | **Impreciso**: 22 son *ocurrencias* (6 definiciones + **16 llamadas**) |
| «la **única** espera de la batería que ya mide por fotogramas» | **FALSA, y es lo que más cambia**. Hay un helper gemelo `frames(ctx, n)` con **9 copias más** — 37:79, 43:65, 58:95, 79:93, 83:66, 86:105, 109:108, 112:67, 126:75 — misma fuente `window.__nefan.fps().frames`, **34 llamadas**, 4 variantes textuales. Censo real: **15 copias en 15 guiones, 50 sitios de llamada** |
| «`reloj()` ya está en el hook (`nefan-hook.ts:262`)» | Está en **`:283`** (`reloj: () => relojDeSim.lee()`). Lo llaman 6 guiones (131, 132, 133, 138, 140, 142) y 2 módulos (`parada.mjs`, `sonda.mjs`): eso sí es exacto |
| «su justificación es HOY más fuerte» | **Más de lo que dice**: el daño que el issue predecía **ya ocurrió**. `#545` aterrizó, `reloj()` se publicó, y las 15 copias siguen leyendo `fps().frames` — que cuenta lo que **PINTA** el renderer (`fps-gl.ts:1536`, `render()` corre también con el título delante) y no lo que **SIMULA** el mundo (`reloj-de-sim.ts:88`, `avanza()` solo se llama si `!titleScreen.isVisible`, `main.ts:664`) |
| «el peaje es re-correr la batería» | **MEDIDO, y es lo que tumba el argumento**: los 6 guiones tardan **42,0 s** (6/6 verde, exit 0); los **15**, **112,0 s** (15/15 verde, exit 0). La batería completa hoy tarda **18–25 min** (seis corridas de hoy: 18m08, 18m11, 18m17, 18m22, 20m00, 25m22). El peaje de la prueba dirigida es **menos de dos minutos**, y está medido **con la batería de otra tanda corriendo a la vez** en esta misma máquina (la de la tanda J arrancó 43 s después que la mía): son techos, no números de laboratorio |

**Y la decisión que esto destapa, que no es un renombrado.** Entre `relojDeSim.frameDelLoop` (`main.ts:562`) y `fpsRenderer.render()` (`:727`) **no hay un solo `return`**: `fps().frames` sube exactamente una vez por vuelta del `gameLoop`, o sea **es `reloj().loop`**, el latido de la página — no `reloj().frames`, que solo sube cuando el mundo se simula. Las 15 esperas no eligieron: heredaron el contador del renderer. *(Escribí antes que migrar podía colgar los cuatro guiones `sinMotor`; **el árbol me lo tumba**: el guion 131 es también `sinMotor`, nunca arranca partida, cierra el título por su verbo y su bloque C afirma que ahí el reloj de sim sigue a la pared. No cuelga a nadie.)* Elegir `loop` o `frames` en cada sitio es del arquitecto; lo que dice el crítico es que **la elección existe y hay que hacerla explícita**.

### #637
| Afirmación | Verificación |
|---|---|
| «el 82 mide una sola rama» | **Cierto**: el libro de B se lee en `82:334` y se loguea en `:338`, y **ningún `ctx.expect` lo mira**. Los dos asertos finales (`:340-344`, `:345-349`) son sobre el CABLE (`rePedidos`, `desdeLaVuelta`) |
| «el único sabotaje que vale es quitar `aspecto.desvestir()` de `main.ts:405`» | **Cierto y en esa línea exacta hoy**. Su cuerpo entero es `playerSkinPrompt = ""` (`aspecto-del-jugador.ts:168-170`): no toca `characterSprites`, y `rearmarCortacircuitos` (`character-sprites.ts:209-214`) solo borra las `failed` — de ahí que la rama «A completo» sea muda |
| «candar la identidad pide un aserto sobre `aspecto.skinPrompt()` … que no existe» | **Cierto, y es MÁS que un aserto**: `aspecto` **no es dependencia del hook** (`DepsDelHook`, `nefan-hook.ts:57-100`), y lo que sí publica (`skins`, `:132-133` → `character-sprites.ts:283-292`) dice quién **PIDIÓ**, no quién **LLEVA PUESTO**. #637 son *dos* piezas: un observable nuevo en el seam + el aserto |
| «tres guiones (27, 82 y 92) compartían el punto ciego» (requisitos, punto 2) | **La ceguera de la tanda F tenía DOS causas**: #627 tocó a **53 y 82**; el `clear()` de la faceta errores tocó a **27 y 92**. Lo único que comparten los tres es mirarse en `#error-log`. Ni 27 ni 92 leen `window.__nefan.skins` ni tocan el aspecto del jugador. **El vecino con el que hay que comprobar el solape es el 51** (mismo `page.route` + 500 sobre `/skin_sprite_sheet`, `51:50-71` ≡ `82:168-208`) **y el 53** |

### #634
| Salida | Verificación |
|---|---|
| **(a)** sacar la posición viva de los NPC de la huella | **BLOQUEADA, y por más de lo escrito**. `huellaDeEscena` (`escena-servida.ts:57-59`) es `JSON.stringify` de la escena entera menos `exits`; el módulo `escena-servida` tiene `break: 100` con **3 mutantes / 0 vivos** (`mutation-targets.json`, `mutacion-huella.json`). Y no es solo el suelo: **`test/escena-servida.test.ts:86-90` afirma literalmente lo contrario** («la posición de un npc distinta → huella distinta») como uno de los negativos que sostienen ese 100. Cambiarlo obliga a borrar ese test y deja huérfano el mutante que hoy mata |
| **(b)** que el guion declare ⊘ | **Inexpresable tal como la escribe el issue**: `run.mjs:1287-1308` (y `:1343-1359`, `:1416-1425`) exigen `fallos.length === 0`, y `veredictos.mjs:33-40` lo remacha. Sí es expresable **declarando ANTES de fallar** |
| **(c)** statu quo | Correcto, y es lo que hay hoy |
| **(d)** *no está en el issue*: congelar al `barkeep` desde el guion | **EXISTE**. Directiva `hold` → `pauseTimer = Infinity` y `move()` no toca la posición (`npc-behavior.ts:431-435`, `:533-539`); canal `POST /npc/{id}/directive` (`state-http/npc-routes.ts:36-42`); **precedente en el banco** (`17-…:209-211`). Cero ficheros de producción, cero riesgo sobre el suelo 100. Queda una ventana de carrera —la huella de referencia se graba en el `scene_init` de arranque (`tile-store.ts:99`), antes de que el guion pueda POSTear—, y esa se cierra con `sinMedirBloque` **antes** del aserto, que ahí sí se honra |
| La vía que trae el coordinador (`sessionId` en `StateUpdateMessage`) | **El defecto es real y lo verifico**: `StateUpdateMessage` (`messages.ts:296-341`) **no lleva `sessionId`** y `game-client.ts` no lo menciona **ni una vez**, mientras `narrative-client.ts:99` y `:153` sí descartan evento y status de otra partida. **Pero no puedo atribuirle el rojo del 75 y nadie debería darlo por hecho**: `state_update` sale con `ctx.send(ws, …)` (`simulation.ts:131`), o sea como respuesta al tick **del propio socket**, no en difusión. Lo que sí puede hacer es contestar a una página que aún no tiene sesión con la que el bridge tenga — que es la firma del **80** («entradas heredadas a una página sin sesión»), no la del **75** (la posición del barkeep dentro de su propia sesión) |

**Lo que MIDO del 75** (cero créditos, el runner eligió bloque libre solo): verde **3/3 aislado** (6,0 · 6,8 · 6,9 s) y verde **3/3 emparejado con su predecesor** el 74 (8,7 · 9,0 · 8,5 s), con huella idéntica (31.070 bytes) y `1 derivación · 1 restauración` en las seis; y verde **3/3 en una ventana de once guiones** (65→75, 87 · 88 · 89 s). Frente a eso, en las **corridas completas de hoy el 75 sale rojo 1 de 7**. **Ninguna de las dos hipótesis queda descartada con esto, y decirlo es el resultado**: la intermitencia solo aparece con la batería entera detrás, lo cual es compatible tanto con «el barkeep tuvo tiempo de andar» (mecanismo verificado: `npc-roles.ts:30` lo hace ambiental, `npc-behavior.ts:425-428` micro-wander, `:700-701` muta `record.position` in situ) como con «llegó estado de otra sesión». **La salida (d) cierra el rojo bajo las dos**, y ésa es la razón de recomendarla: no obliga a ganar antes la discusión sobre la causa.

## El día después

- **#606** — una definición en `qa/lib/` con su test (precedente exacto: `parada.mjs` ↔ `test/parada-de-qa.test.ts`), y **15 definiciones locales que se borran**. No se pierde vigilancia: `esperas-que-conducen.test.ts` lee **TODO `qa/**.mjs`** (`:262-272`), no solo `guiones/`, así que el helper sigue bajo el detector — es lo contrario de la enfermedad que denuncia #655. Lo que hay que decidir y **no** puede hacerse por descuido: los cortafuegos son **10 s** en las 6 y **20 s** en las 9. Unificar hacia arriba; **bajar ninguno**.
- **#637** — el hook gana un observable y el 82 gana un bloque. Lo que se vuelve más difícil: el 82 pasa a medir **dos ramas** (con y sin el 500 sobre A) en una sola página con dos partidas; es el guion más caro de montar del banco (bridge propio, disco efímero, 90 s de espera de puerto, `82:100-128`). Si no cabe, la mitad nueva es un 82bis que paga ese montaje otra vez — decisión del arquitecto, no mía.
- **#634 con (d)** — el 75 pasa de moneda al aire a candado. No pierde poder: el aserto `:260` sigue pudiendo ponerse rojo si las salidas entraran en la huella (es lo que el guion añade entre las dos difusiones). Lo que **no** arregla, y hay que escribirlo al cerrar: la posición viva sigue en la huella (#467, cerrado `not_planned` a conciencia) y el `sessionId` sigue faltando en `StateUpdateMessage` — **eso merece issue propio**, y su sujeto vivo es el guion 80.

## Conflictos

- **#606 ↔ #610 · solapamiento con orden.** #610 mira **los mismos 15 helpers desde el otro lado** y los declara «el conjunto que **NO** debe marcarse» (`esperas-que-conducen.test.ts:132-143`). Hacer #606 con los 15 **abarata** #610: el predicado queda en un sitio auditable en vez de nueve. Hacer #610 antes deja su censo obsoleto el día siguiente. **Orden: #606 → #610.** De paso, el censo escrito en el árbol dice **7 guiones** y son **9**: le faltan el 79 y el 126, porque contó otra pregunta (los sitios con tecla puesta).
- **#634 ↔ #467 · contradicción aparente, no real.** (a) desharía lo que #467 decidió al cerrarse `not_planned`. (d) no.
- **Tanda J** toca 118/119/120/128 y quizá `run.mjs`: **cero solape** con 16/22/30/31/44/45, 37/43/58/79/83/86/109/112/126, 82 y 75. **Tanda K** toca `nefan-html/src/main.ts:514`; **#637 toca `main.ts` también** (para pasar `aspecto` al hook) — mismo fichero, otras líneas: riesgo de merge bajo pero real, y quien lo implemente debe saberlo.

## Coste contra valor

- **#606** — valor alto (15 sitios pasan a uno, y el banco deja de tener dos poblaciones de espera: 6 guiones en el reloj de sim y 15 en el contador del renderer), coste medido **112 s** de prueba dirigida. Si no se hace nunca: la próxima vez que se toque la espera por fotogramas son 15 ediciones a mano y el que se quede atrás sale verde. **Cabe.**
- **#637** — valor alto y del jugador (aparecer con la armadura del personaje anterior es visible), coste medio: un observable nuevo + un bloque en el guion más caro de montar. Si no se hace nunca: el 82 seguirá prometiendo «volver al título desviste» y midiendo la mitad.
- **#634 por (d)** — valor alto por precio bajo: devuelve un candado a la batería sin tocar producción. **No hacer nada** aquí es lo peor de las cuatro: un rojo que aparece y desaparece enseña al equipo a ignorar la batería, que es el mismo destino de un gate que nace rojo.

## Qué cambiarle a `requisitos.md`, para pegar tal cual

1. **Tabla de los tres issues, fila #606** → «`esperarFrames` está copiado en seis guiones **y su gemelo `frames(ctx, n)` en otros nueve**: son **15 copias de la misma espera por fotogramas en 15 guiones, con 50 sitios de llamada**, y ninguna tiene dueño en `qa/lib`.»
2. **§ #606, primer párrafo** → «Confirmado al abrirlo y **re-medido hoy**: 6 copias de `esperarFrames` (16 llamadas) **más 9 de `frames(ctx, n)` en los guiones 37, 43, 58, 79, 83, 86, 109, 112 y 126 (34 llamadas)**. `reloj()` está en `nefan-hook.ts:**283**` y lo usan 6 guiones y 2 módulos de `qa/lib/`.»
3. **§ #606, «el peaje»** → «**El peaje está MEDIDO y no es la batería**: los 15 guiones afectados corren en **112 s** (15/15 verde, exit 0); los 6 originales, en **42 s**. La batería completa son 18–25 min y se paga una vez al cerrar, como en toda tanda. **Y una premisa que hay que medir antes de elegir fuente**: cuatro de los seis (16, 22, 30, 44) son `sinMotor` y nunca arrancan partida, así que migrar el predicado a `reloj().frames` puede colgarlos.»
4. **§ «Lo que el crítico tiene que decidir», punto 2** → «¿#637 se arregla en el 82 o en un guion nuevo? **El vecino con el que hay que comprobar la ceguera compartida es el 51** (mismo `page.route` + 500 sobre `/skin_sprite_sheet`) **y el 53**, no el 27 ni el 92: la ceguera de la tanda F tuvo dos causas distintas y lo único que compartían los tres era el `#error-log`. **Y el aserto que pide el criterio de cierre no es expresable hoy**: `aspecto` no es dependencia del hook, así que #637 son dos piezas — un observable nuevo en `nefan-html/src/dev/nefan-hook.ts` y el aserto en el 82.»
5. **§ #634 entero** → «**#634 NO está bloqueado.** Su (a) sí lo está, y por más de lo escrito: además del `break: 100` de `escena-servida`, el test `escena-servida.test.ts:86-90` afirma literalmente que la posición de un npc entra en la huella, y es uno de los negativos que sostienen ese 100. Su (b) es inexpresable *declarando después de fallar*, y su (c) es rendirse. **Existe una (d) que el issue no lista**: congelar al `barkeep` con `POST /npc/barkeep/directive {type:"hold"}` desde el guion — mecanismo en producción, precedente en el 17, cero ficheros de producción y cero riesgo sobre el suelo. La ventana de carrera del `scene_init` se cierra con `sinMedirBloque` *antes* del aserto.»
6. **§ Restricciones, añadir** → «**Que falte `sessionId` en `StateUpdateMessage` es un defecto real y verificado** (`messages.ts:296-341` sin el campo; `game-client.ts` sin una sola mención, frente a `narrative-client.ts:99`/`:153` que sí filtran), **pero su sujeto vivo es el guion 80, no el 75**: `state_update` se manda con `ctx.send(ws, …)`, como respuesta al tick del propio socket. Va a issue nuevo; no entra en esta tanda y no se usa para justificar el rojo del 75.»
7. **§ Restricciones, añadir** → «**Nada de esta familia se mide con la batería completa**: guion aislado, pares y ventanas cortas, tres corridas de cada. Y el orden entre tandas: **#606 antes que #610**.»
