# Crítica 2 · tanda D «el reloj y la batería», segunda mitad

**#441 EN CONFLICTO** (su solución no es la palanca, y hacerla primero inutiliza el veredicto de la otra) · **#597 VIGENTE, con su vía 1 FALSADA y una tercera medida** · **#545 REENCUADRADA otra vez** (el alcance de ayer estaba mal, y el fallo caro va en la dirección CONTRARIA a la que el issue nombra) · **#599 no es un vecino: es un BLOQUEANTE de la tanda** · **#598, #596, #430 fuera a propósito**. Verificado contra `main` @ `a580df90` leyendo el código, los tres informes en disco y los tiempos de los dos jobs en Actions; sin correr mutación y sin arrancar nada. Construida encima de `critica.md`, que no repito — y de la que corrijo tres citas mías.

**El problema real, en una frase:** el módulo más caro está al **91,7 % del techo de su job** contra una deriva medida de ×1,14, así que un día malo no da un lote lento: da una corrida INCOMPLETA con el tag quieto — y hay dos palancas que lo arreglan, una de las cuales no se puede juzgar hoy porque la herramienta que la juzga tiene una condición que solo sale en una dirección.

---

## 1 · ¿La tarea de #441 es partir el fichero o arreglar los 26? **Ninguna de las dos hoy.**

**Premisa (a) · partir `scene-validate.ts` es posible, y lo que arrastra no es código: es la MEDIDA.** El fichero son 1.026 líneas con costuras limpias (`openTile` :270 · `composePlan` :418 · `buildWalkableMap` :452 · los cinco `check*` · `floodFill` :809 · `validateScene` :1003) y **un solo símbolo cruza la frontera**: `validateScene`, importado por `bridge/handlers/tile.ts:14`, `bootstrap-tile.ts:11`, `state-http/scene-routes.ts:12` y `src/games/world-snapshot.ts:24` (más los tipos re-exportados en `src/contracts/common.ts:109`). Un movimiento puro no toca a ninguno. Lo que arrastra es otra cosa, y no está en el issue:
- el fichero nuevo entra en el perímetro de la totalidad → **módulo nuevo con `break: "sin medir"`**, y un primer suelo no se puede poner en local (`permisoLocal` rechaza el coste desconocido): **hace falta una corrida autorizada solo para eso**;
- y sobre todo: `veredictoDeAdopcion` (`scripts/mutacion-huella.ts:512`) declara `adopta = peros.length === 0` con **`incomparablesPorCodigo`** y **`sinBase`** entre los motivos. Partir el fichero deja el viejo incomparable por código y los dos nuevos sin base → **la siguiente comparación de adopción sale NO, por un motivo que no es del runner.** O sea: **partir el fichero y juzgar un runner no caben en el mismo ciclo.** Son dos corridas autorizadas, no una.

**Y no compra lo que parece.** El lote de `scene-validate` tardó 43 de los 44,5 min — pero **el segundo lote más largo de esa misma corrida fue de 35 min** (`medir (4, blueprint-volumenes world-map npc-director apuntado)`, run `34872537438`), con `max-parallel: 6`. Partirlo en dos mitades de ~1.300 s no lleva la corrida a 22 min: la lleva al techo del siguiente lote, **~35 min = −21 %**, y **0 % de reloj de CPU**. La otra vía está medida en **22,4 min y −63,4 % de CPU**.

**Premisa (b) · los 26 son exactamente lo que dice #597, y la contabilidad es exacta.** Contados en los informes de disco: `contrato-sprite-forge` 5 · `blueprint-suelo` 6 · `blueprint-derive` 2 · `blueprint-volumenes` 12 · `state-http-dispatch` 1 = **26**, y en los cinco ficheros **cada `RuntimeError` salió de `Killed`** (p. ej. `blueprint-volumenes`: Killed 277→265 y RuntimeError 12; Survived 98→73+25 `NoCoverage`). Nada más se movió. Miré dos de verdad: `ground.ts:43` es `GroundPathSchema = z.object({…})`, y con `{}` el `z.discriminatedUnion("kind", …)` de `ground.ts:104` **revienta al evaluar el módulo**; `http.ts:43` es `opts?.mutates` → `opts.mutates`, y `endpoint()` se llama al construir la tabla de `world-state.ts`. Muerte durante la carga, sin una línea de TAP.

**Pero la vía 1 que el issue propone NO funciona, medido.** `captureTapResult` (`tap-helper.js`) lanza si `exitCode !== 0 && !failedTests.length`, y `failedTests` se llena solo con líneas `not ok`. Hice que el fichero emitiera TAP antes de morir (un `--import` que registra un test): salen `TAP version 13`, `ok 1`, `1..1`, **`# fail 0`** y exit 1 → sigue siendo `RuntimeError`. Y el corolario es peor que el problema: si alguien además neutraliza el exit code, esos **26 mutantes muertos pasan a contarse como SUPERVIVIENTES**. Es el aserto que cubre menos de lo que promete, en su forma más cara.

**Hay una tercera vía, y me la encontré midiendo: una flag.** `node --import tsx --test --test-isolation=none --test-reporter=tap <fichero>` sobre un fichero que muere al importar emite `TAP version 13` y **`not ok 1 - <fichero>`** → `failedTests.length = 1` → el runner lo clasifica `Failed` → **mutante `Killed`**. Y `--test` deja de estar prohibido porque con `--test-isolation=none` **hay UN solo pid** (medido: 1 pid con la flag, 2 sin ella), que es justo lo que el hook de `tap-runner` necesita. Coste: **+9 ms por proceso sobre 231 (+3,9 %)**, sin `ExperimentalWarning` en Node v24.11.1. No lo he probado con `tap-runner` instalado ni con cobertura real: eso es del arquitecto.

**Y lo decisivo para el orden: esto se contesta en LOCAL, en 11 segundos.** `contrato-sprite-forge` son **63 mutantes** (cabe de sobra en `tope_local` 120), **11 s medidos**, y **5 de los 26 viven ahí**. Antes de partir un fichero de producción hay una medida que cuesta menos que leer este párrafo.

> **Corrección a la cifra que el issue publica hoy**: `presupuestoDelLote(2594)` = `round(2594×1,25)+60` = **3.302 s = 91,7 % del `techo_job`**, y el crecimiento tolerado es **×1,090** — no 3.285 / 91,3 % / ×1,110 (requisitos y mi crítica anterior). Más apretado, no menos. `scene-validate` = 2.594 s de 12.492 = **20,8 %**: esas dos sí están bien.

> **Y una que se quedó sin sujeto**: el comentario del 2026-09-04 de #441 dice que «partir la batería ataca el 30 %, no el 70 %». La corrida con `perTest` —que es «batería de uno»— bajó ese módulo un **50,6 %**. El modelo `T ≈ 5 s + mutantes × batería` está **falsado**: el tiempo del mutante lento se paga DENTRO de cada fichero de batería, así que también es proporcional a ella.

> **Los cuerpos de #441 y #430 siguen mintiendo.** Las correcciones se publicaron como COMENTARIO y el cuerpo sigue diciendo 2.510 s / 826 mutantes / «medir si `perTest` compensa» (#441) y ~900 mutantes / 18 ficheros / «arriesga el `timeout-minutes: 180`» (#430). Quien lee `gh api …/issues/N --jq .body` —que es lo que hace un agente— lee lo falso. **Editar los cuerpos, no añadir otro comentario.**

## 2 · El bloqueante: **#599 va PRIMERO o la tanda no puede decidir nada**

`veredictoDeAdopcion` exige las siete, y la séptima es `sinEjercer = 0`. La corrida con `tap-runner` dio **1122**, y #599 explica por qué eso no se puede cumplir nunca: con `coverageAnalysis: "off"` la base **no puede** emitir `NoCoverage`. **No hay flag que lo esquive** (lo leí: `adopta = peros.length === 0`, sin escape). Consecuencia dura: si se arreglan los 26, se lanza otra corrida y `comparar` **vuelve a decir NO** por los mismos 1122, y se habrá gastado una corrida autorizada para nada.

Y se arregla sin correr nada: **cada informe de módulo lleva su `config.coverageAnalysis`** (`"off"` en `reports/mutation-base`, `"perTest"` en `reports/mutation`). La base sabe decir si podía expresar `NoCoverage`. Es `scripts/` y su batería, cero producción, cero runner.

## 3 · #545: el alcance estaba mal, y el issue apunta al lado que no duele

**No son 30 de 112: son 48 de 129** (37 %), recontados fichero a fichero: **20** llegan por `acercarse`/`herirHasta` (`qa/lib/combate.mjs:87,99,124`) y **28** a mano. Perímetro estricto (solo posición y HP del jugador, sin wind-up ni NPC ambiental): 43. El clamp sigue vivo pero **se ha movido**: `nefan-html/src/main.ts:566`, no 577. Es una corrección **hacia arriba**, y la razón de que crezca es lo segundo que hay que decir.

**Los tres guiones que la tanda de ayer añadió o tocó heredaron el patrón, y con el presupuesto más apretado de la batería**: `90-…:331`, `129-…:292` y `130-…:321` son el mismo `holdUntil("up", …, 8_000)` esperando **0,5 m andados en 8 s de pared**. El defecto se reproduce solo, PR a PR, porque nada lo sujeta.

**Y el sujeto no son «los asertos»: es UN patrón, y está en el guion que #545 manda investigar.** El 91 detecta «el jugador se para» así (`guiones/91-…:146-172`): muestrea `state().pos` **cada 150 ms de reloj de pared** (la cadencia de `waitFor`, `qa/lib/sonda.mjs:121`) y declara parada cuando **tres muestras seguidas** se mueven menos de 0,02 m. Bajo carga el rAF no corre entre dos muestras: el jugador no se ha movido **porque no ha habido frame**, y el guion lee una parada que no existe. Es literalmente el título del issue, y **contesta su primera pregunta —«saber en qué falló el 91»—, que nadie había contestado.** El patrón exacto (`quieto`) está en **13** de los 48: 41, 42, 46, 62, 67, 73, 88, 91, 93, 110, 111, 113, 126.

**Y hay una mitad que va al revés de como está escrito el issue.** `qa/README.md:130` lo dice él mismo: «`debeOcurrir:false` es **el timeout ES el éxito**». Bajo carga el timeout está garantizado, así que esos asertos se vuelven **VERDES sin haber mirado**: dentro de los 48 son **2 enteros (02:62, 30:147) y 2 mitades** (06:67, 45:70, donde el signo es dato), más **14 guiones** que se tragan una expiración con `ctx.absorbe`. El rojo intermitente es la mitad ruidosa y barata; **la silenciosa no se ve en un 129/129**.

**El reproductor NO es el lujo** — me corrijo: es lo que hace falsable la distinción con #496, y los 48 están identificados **por inspección, sin un solo rojo reproducido**. Sigue sin existir nada en `qa/` con que meter carga (cero `setCPUThrottlingRate`, `newCDPSession`, reloj falso). Y `qa/README.md:149-155` **ya escribe hasta el remedio** («se arregla haciendo que el juego lo RECUERDE —un recuento en `debugState()`—, no bajándole el listón al guion»), aunque solo para el caso inverso (el estado transitorio). El criterio 5 está casi hecho; lo que falta no es documentación, es que nada lo sujeta.

> Corrijo tres citas mías de ayer: `holdUntil`/`expectEspera` **no** viven en `qa/lib/sonda.mjs:100-133` sino en `qa/run.mjs:855` y `:948`; la regla del reloj de `qa/README.md` está hoy en **140-155**, no en 120-134; y `holdUntil` no es el defecto — «`maxMs` es un cortafuegos, no la condición de parada», y es cierto.

## 4 · Cómo se distingue un verde de #545 de un verde que tapa #496 (la pregunta que vale la tanda)

**Sí se distingue, y la distinción es POR ASERTO, no por guion. Los dos rojos tienen formas distintas y las dos se leen en el código.**

- **#496 es siempre un CONTADOR sobre un canal compartido.** En el 80: `:148` — `"…ni deja una entrada de error"`, una igualdad de conteo de `#error-log` tomada antes (`:142`) y después (`:145`). En el 75: `:222` — `"sin re-difundir la escena (cero scene_init desde el link)"`, un **conteo a cero** sobre los frames de websocket que el propio guion recoge (`:173-175`). Un `scene_init` o una entrada de la partida anterior caen dentro de esa ventana y la tumban sin nombrar a quién.
- **#545 es siempre un PRESUPUESTO en ms para que el juego progrese.** En el 80: `:116`, `"el jugador anda PASO_M m"` con `{ ms: 15_000, tecla: "up" }`. El 75 **no tiene ninguno**: sus esperas son del bridge (`esperarLlegada`, `exits_changed`). **Así que el 75 no puede ponerse rojo por carga: si el 75 sale rojo, es #496 y punto.**
- Tres controles ejecutables, en este orden: **(1) el texto del aserto** que se pone rojo dice cuál de las dos es; **(2) el orden** — #496 solo aparece en batería y el 80 es *verde en solitario siempre* (medido en el issue), mientras que #545 se reproduce en solitario **con carga**; **(3) el diff** — #496 solo se tapa de una manera: relajando *qué* se cuenta. Regla dura y comprobable en revisión: **esta tanda solo puede cambiar CÓMO se espera, nunca QUÉ se cuenta ni sobre qué canal.**

**Dónde NO se distingue, y por eso es un límite y no una receta:** si la contaminación *causa* la inanición (la página gasta frames digiriendo tráfico de otra sesión), las dos dan el mismo síntoma en el aserto de reloj. Separarlas pediría **atribuir** cada frame a una sesión, y eso hoy no se puede: ver los frames sí (el 75 lo hace), pero `state_update` sale **sin sellar** por `ctx.send` (`bridge/handlers/simulation.ts:131`; solo `broadcastNarrative`/`enviarNarrativo` sellan, `bridge/ws-server.ts:130-136`), el cliente no filtra por sesión en ningún sitio, y `aisla` solo acepta `saves|mundo|fake-ai` (`qa/run.mjs:730`): **no hay forma de pedir un bridge sin sesión viva.** Conclusión operativa: **80 y 75 quedan FUERA, por escrito** — y con ellos **#497** («el registro no se vacía al cambiar de partida»), que es una TERCERA causa de rojos del mismo contador y que el aviso del coordinador no nombra.

## 5 · El día después, coste y qué NO hacer

- Si se parte el fichero: dos módulos sin suelo, una corrida gastada en ponérselo, `mutation.yml:150-162` con su prosa a reescribir, y **el reloj de CPU igual que hoy**. Dentro de un mes, un fichero partido por el planificador de lotes y nadie recordará por qué — sobre todo si después se adopta el runner y la partición deja de hacer falta.
- Si se arregla #597: no se toca producción, `#441` se disuelve (1.242 s → presupuesto 1.613 s = 45 % del techo), **#430 se contesta sola** y #598 pasa a ser candable. Si no se arregla, #441 es la tarea y hay que hacerla.
- **No hacer nada** cuesta: una corrida entera (44,5 min, 12.492 s de CPU) a ×1,09 de volverse INCOMPLETA. No es «el reloj»: es repetir los 57 módulos que sí midieron.
- **Lo que no se hace, pase lo que pase:** subir `tope_lote`, `techo_job` o `timeout-minutes`; recortar a mano ninguna batería; tocar producción para que el instrumento mida más cómodo; y tocar los guiones 80 y 75.

## 6 · Orden, en PR abandonables — y qué se queda fuera a propósito

1. **#599** · la séptima condición deja de votar cuando la base no sabe decir `NoCoverage`, y el bloque se sigue imprimiendo. Cero producción, cero runner. **Sin esto, 2 no puede terminar.**
2. **#597 en local** · la vía medida sobre `contrato-sprite-forge` (63 mutantes, 11 s, 5 de los 26) y luego `blueprint-derive`/`blueprint-suelo` si cabe. Dos salidas buenas: los 5 vuelven a `Killed` → se pide la corrida; no vuelven → #597 se cierra **con el número** y #441 pasa a ser la tarea del día siguiente.
3. **#545** · el reproductor primero (sin él los 48 son inspección, no medida), luego los **13** del patrón `quieto` y los tres de `8_000 ms / 0,5 m` (90, 129, 130). **Sin tocar 80 ni 75**, y sin cambiar ni un aserto de conteo.
4. **#441 no entra.** Su problema es real y urgente, pero su solución cuesta dos corridas autorizadas y compra −21 %; la decisión de hacerla es la SALIDA de 2, no su entrada.

**Fuera a propósito, para que no parezca olvido:** **#598** (sus 1122 solo se pueden candar con una corrida que emita `NoCoverage`; depende de 2 — lo único que se puede hacer hoy sin nada es `place-target.ts`, 35/35, y eso es deuda de tests, no de reloj) · **#596** (candado general del score `NaN`; su puerta concreta ya está cerrada en `feature/tap-runner` y no bloquea a nadie) · **#430** (es del usuario, y su respuesta la escribe 2) · **#496 y #497** (causas raíz de los rojos del registro: se nombran para no taparlas, no para arreglarlas aquí).

## 7 · Qué le cambiaría a `requisitos.md` (redactado para pegarse)

1. **Sustituir el punto 1** por: «**#441 no entra en esta tanda.** Su problema es real —`presupuestoDelLote` = 3.302 s = **91,7 % del `techo_job`**, margen ×1,090 contra una deriva de ×1,14— pero partir el fichero compra **−21 % de pared** (el segundo lote de la corrida base fue de 35 min) y **0 % de CPU**, y cuesta **dos corridas autorizadas**: el módulo nuevo nace sin suelo, y `sinBase` + `incomparablesPorCodigo` son condiciones duras de `veredictoDeAdopcion`, así que partir el fichero y juzgar un runner **no caben en el mismo ciclo**.»
2. **Añadir como punto 0, bloqueante**: «**#599 va primero.** `adopta = peros.length === 0` y la séptima condición es `sinEjercer = 0`; con la base en `coverageAnalysis: "off"` esos 1122 vuelven a salir y tumban cualquier corrida futura por un motivo que no es del runner. Se arregla leyendo `config.coverageAnalysis`, que **ya viaja en cada informe**. Sin runner y sin producción.»
3. **Reescribir el punto de #597**: «La vía 1 del issue —que el fichero emita TAP antes de morir— **está medida y no funciona**: `captureTapResult` lanza si `exit ≠ 0` y no hay ninguna línea `not ok`, y un TAP limpio con exit 0 convertiría 26 muertes en 26 supervivientes. Hay una tercera vía medida: `--test --test-isolation=none` en `tap.nodeArgs` (un solo pid, `not ok` en la muerte al importar, +3,9 % de arranque). **Y se contesta en local y en 11 s sobre `contrato-sprite-forge`** (63 mutantes, 5 de los 26), antes de tocar nada.»
4. **Sustituir el alcance de #545** por: «**48 de 129 guiones**, no 30 de 112 (estricto: 43); 20 por `acercarse`/`herirHasta` y 28 a mano. El clamp está en `main.ts:566`. El sujeto es **un patrón, no los asertos**: leer una parada muestreando la posición cada 150 ms de pared y contar tres muestras quietas — **13** de los 48 (41, 42, 46, 62, 67, 73, 88, 91, 93, 110, 111, 113, 126), y es lo que falló en el 91 (`guiones/91-…:146-172`). **Los tres guiones que la tanda de ayer añadió o tocó (90, 129, 130) heredaron el presupuesto más apretado de todos: 0,5 m en 8 s de pared.** Y una mitad va al revés: `debeOcurrir:false` hace del timeout un éxito (02, 30 enteros; 06, 45 a medias) y `ctx.absorbe` se lo traga en 14 más — bajo carga eso es **verde**, no rojo. **El reproductor va PRIMERO**: hoy los 48 son inspección, sin un rojo reproducido.»
5. **Sustituir el aviso de #496** por: «**80 y 75 quedan fuera, por escrito**, y con ellos #497. La distinción es **por aserto**: #496 es siempre un CONTADOR sobre un canal compartido (80:148, 75:222) y #545 es siempre un PRESUPUESTO en ms para que el juego progrese (80:116). **El 75 no tiene ninguno: si sale rojo, no es carga.** Controles: el texto del aserto · correr el guion el primero · y el diff (**solo puede cambiar CÓMO se espera, nunca QUÉ se cuenta**). Donde NO se distingue —contaminación que causa inanición— falta atribuir cada frame a su sesión: `state_update` sale sin sellar (`ctx.send`) y `aisla` no sabe pedir un bridge sin sesión viva.»
6. **Añadir a los avisos**: «Los **cuerpos** de #441 y #430 siguen publicando las cifras falsas; las correcciones están solo en comentarios. Editar los cuerpos.»
