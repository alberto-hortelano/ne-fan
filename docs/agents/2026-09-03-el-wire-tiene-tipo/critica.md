# Crítica — T7 «El wire tiene tipo» · medido sobre `4ca0c50` el 2026-09-03

**Veredicto: #378 VIGENTE (mayor de lo escrito) · #399 VIGENTE · #397 REENCUADRADA (mayor: espejo Python y tipos del core que el issue no cita; el save ya conserva las dos) · #400 REENCUADRADA: restos 2 y 3 VIGENTES, resto 1 PREMATURO y sale de la tanda.** El orden «#378 primero para que los otros nazcan tipados» tiene la premisa rota: los otros tres tocan la ENTRADA (zod de Format D, effect de spawn), y `formatDToWorld` no emite `glyph` ni `attach`. Son independientes; lo que importa es el corte, no el orden.

## El problema real, en una frase
La forma que más viaja (motor → bridge → cliente → save) está sostenida por convención en tres sitios —un `Record` sin miembros, dos vocabularios de entity y campos que el motor puede escribir sin que nada los lea—, y cada tanda paga la conversión a mano.

## Premisa, afirmación por afirmación
**#378.** Cierto y corto. Censo de quién abre la world scene con `as`: cliente **14** (`world/carga-de-tile.ts:272,279,280,293,301,302,312,342`; `main.ts:324,2022,2029,2031`; `ui/style-apply.ts:214` doble cast, más su copia privada de la forma `WorldSceneDelBatch:40-45`), bridge **1** (`sim-collision.ts:77`), core **0** casts pero **6 firmas** sobre `Record<string, unknown>`/`unknown` (`scene-normalize.ts:95`, `mundo-persistido.ts:250,328`, `entidades-del-tile.ts:260,275,292-300`, `wire-scene.ts:96,114`, `context.ts:290`), y tres tipos del wire que mienten: `messages.ts:304` (`scene?: Record`), `narrative/types.ts:364` (`Effect.data: Record` lleva `{scene}`), y **`SessionData.scenes_loaded` sale por el wire con world scenes dentro bajo el tipo de Format D crudo** (`types.ts:86-96` + `wire-scene.ts:143`). `POSICION_DECLARADA` sigue (`mundo-persistido.ts:108,263,330`).
Lo que emite `formatDToWorld` (`:236-278`), 20 claves: siempre `scene_id, scene_description, dimensions, world_rect, terrain, terrain_grid, objects, npcs, __format_d`; según origen `tile, ground, volumes, scatter_*, biome, ambient_event, __plan, __plan_warnings, __player_start` (null sin entity player). Encima, en el wire: **`exits` sí es campo de la escena servida** (`wire-scene.ts:105`) y el cliente lo lee y lo ESCRIBE en su copia (`carga-de-tile.ts:143,164`) — «ya no es campo» vale para lo persistido, no para el wire; y por npc `position_declared` + `combat.health/max_health` (`mundo-persistido.ts:261-275`). Lectores de `__`: `__plan` → `carga-de-tile:301`, `style-apply:232`, `sim-collision:78` (es contrato de render, no marca); `__plan_warnings` → `carga-de-tile:302`; `__player_start` → `carga-de-tile:342` (solo primer tile o no-grid); **`__format_d`** → un lector, `main.ts:2029`, para sacar `place_id`, y `formatDToWorld:100` como marca de idempotencia. Medido: `__format_d` es el **44-45 % de los bytes** de un tile en el wire (robledo 49.104 → 27.364 sin él; el grid 128×128 viaja dos veces, en `terrain` y en `terrain_grid`), y `tile-store.ts:83` lo mete en la huella `JSON.stringify(scene)`.
Puerta browser-safe: **ya existe**. El cliente importa `@nefan-core/src/scene/scene-normalize.js` en 3 sitios; `src/scene/**` y `src/session/**` están bajo `core-puro-sin-node`. Nada que abrir; #359 (barrel) es aparte.
**#397.** Cierto (`schemas.ts:36-41`, `main.ts:1900`). Menor de lo escrito en una cosa: **el save ya conserva las dos** — `recordEntitySpawned(…, data = c)` guarda la consecuencia entera (`consequence-handler.ts:117-130`) y `spawnsDeRuntime` rehidrata `name`/`description` desde `data` (`mundo-persistido.ts:487-497`); el guion 41 ya manda ambas (`fake-ai-server.ts:463-495`). Mayor en cinco sitios que el issue no cita: `narrative/types.ts:131-143` y `:356-364` (`description` obligatoria, `name?`, e índice `[key: string]: unknown`), `consequence-handler.ts:95` (`c.description ?? "an entity"`: default mudo), `mundo-persistido.ts:436-443` (`SpawnDeRuntime`), **`ai_server/narrative_schemas.py:1048-1056`** (espejo Python: rechaza sin `description`, `name` opcional — sin tocarlo la vía API directa sigue diciendo lo contrario), y `bridge/context.ts:334-340` (el `scene_init` va envuelto en un effect `spawn_entity` con `description = scene_description` y sin `name`: si el effect exige `name`, el envoltorio revienta). `narrative_event.md:111-119` y `narrative_react.json:65-107` se RENDERIZAN del zod (`schemas.ts:199`, `contract-prompts.test.ts:52`): siguen solos. `EntitySchema` ya tiene `description` con el refinamiento de procedencia (`scene-schema.ts:126-131`).
**#399.** Cierto. Escriben `"W"` en `src/scene/**`: **cero productores** (`scene-normalize.ts:91` lo declara en `DEFAULT_SOLID_CHARS`, `scene-expand.ts:36` lo busca, `terrain-collision.ts:4` y `tile-edges.ts:37` lo comentan). El único grid del repo con `W` es el que escribe a mano `test/scene-expand.test.ts:30-41`. `attach` en snapshots/saves/labs/qa-datos: **0** (no hay snapshots en el árbol: `data/games/*/world` = 0 ficheros). Sitios vivos: `scene-schema.ts:81`, `scene-expand.ts:32-36,45,179-195`, `generate_scene.json:125-131`, `scene_instructions.md:51`, `tile_instructions.md:321` (prosa «DECOR ATTACH, GLYPH»), `narrative_schemas.py:831-832`, `test/scene-expand.test.ts:45,62-68`, `test/scene-schema.test.ts:400`, y **`qa/contrato-candados-en-negativo.mjs:65-69`**, un invariante en negativo que borra `attach` del zod y espera rojo: al retirar el campo hay que reapuntarlo a otro o nace roto. Derivado que la tarea no dice: al retirarlo, **`W` queda en `DEFAULT_SOLID_CHARS` con cero productores** (misma familia); y `hasUnexpandedPrimitives` se reduce a `tile !== undefined`, con lo que la «expansión defensiva» de escenas sin tile muere con él.
**#400.** Resto 1 — **premisa rota**: la rama «centrada en el origen» tiene tres sujetos vivos. (a) **Es el camino principal de los tests**: `scene-normalize.test.ts:19-41` (`makeFormatD`, 10×6 sin `tile`) —la batería de los 229 mutantes corre ENTERA por la rama centrada—, `scene-expand.test.ts:26-47` (12×12), `helpers.ts:58-70` (`escenaExpandidaDePrueba`, 4×4, usada por **11** ficheros de test), `sim-collision.test.ts:58-69`. (b) **El gate del loader la admite**: `tile` es opcional en `sceneBaseShape` (`scene-schema.ts:42`) y solo el `superRefine` de `EmittedSceneSchema` lo exige (`:252`); `ExpandedSceneSchema` (`:329-347`) no, así que un save con escena sin tile es legal (`types.ts:90-92` lo documenta como «legacy accesible por TravelPanel»). (c) **El cliente tiene su propio camino no-grid**: `carga-de-tile.ts:272-290,340-346,369-382` (`loadRoom`), `tile-store.ts:23-26,64-72`. Además `formatDToWorld:107` devuelve el crudo si no es Format D citando `change_scene`, que ya no existe (`grep` → 0). Borrarla es: `tile` obligatorio en el loader, cuatro fixtures reescritas a 128×128 (cambian todas las coordenadas esperadas de `scene-normalize.test`), y retirar el camino no-grid del cliente. No son cinco líneas: es una tanda.
Resto 2 — `:245` es `EmittedSceneSchema` (motor); **`:341` es `ExpandedSceneSchema`**, el gate del loader (saves y snapshots, `narrative-state.ts:488,600`, `world-snapshot.ts:48`). Lo que sostiene cada uno: `ambient_event` (3/3 fixtures de `data/scenes`; **el saneador Python lo escribe SIEMPRE**, `narrative_schemas.py:540`: un `.strict()` sin declararlo rechaza toda escena que pase por ai_server), `place_anchors` (`:580-591`; lectores `tile.ts`, `bootstrap-tile.ts`, `bootstrap-place.ts`, `narrative-state.ts`); y solo en el loader lo que el bridge ESTAMPA sobre el crudo: `place_id` (`tile.ts:135`, `bootstrap-tile.ts:78`) además de `size`/`terrain`/`__expanded` ya declarados. El «18 de 20 tiles con `style_ref`» del comentario (`:326-328`) es de snapshots que no están en el árbol: pre-producción, se regeneran.
Resto 3 — cierto, pero no son 16 fixtures: `glyph` está en **17 JSON** (14 de `data/contract/fixtures/scene/` + 3 de `data/scenes/`), **98 ocurrencias en 25 tests** de core, `generate_scene.json:107,158` (required), `scene_instructions.md:13,153`, `tile_instructions.md:299,321`, `narrative_schemas.py:769-779` (lo EXIGE) + 3 tests Python, `labs/narrative/fake-scenes.ts` y `check-scene.ts`, `qa/presupuesto-de-volumenes.mjs`, guiones 40 y 46, y **`CLAUDE.md:183`**. Como `EntitySchema` es `.strict()` (`:146`), cada fixture de test que pase por el zod se pone roja al quitarlo del shape: es mecánico, pero son ~120 ediciones, no «retirar del tipo».

## El día después
Para quien juega: nada visible, salvo #397 (un edificio spawneado sin `name` deja de rotularse con su descripción larga). Lo que se vuelve más difícil: nada real; lo que se cierra es escribir `position_declred`. Lo que nadie borrará si no se dice: `__format_d` entero en el wire (44 % de bytes por un `place_id`) y `W` en `DEFAULT_SOLID_CHARS`. Lo que parecerá arbitrario en un mes: un tipo `WorldScene` con `exits?` «a veces» — la decisión wire-overlay vs. miembro hay que escribirla en el tipo, no en un comentario.

## Conflictos
- **#404**: esta tanda toca `data/contract/` en cada PR (`tools/generate_scene.json`, `narrative_react.json`, 3 prompts, 14 fixtures, `arch-rules.json`, y `client-file-size.json` si `main.ts` pierde líneas). Cada PR fuerza la corrida completa (~3 h) y la atribución colapsa a «todas para todo». Asumirlo, no fingir que `local` sirve.
- **Mutación local: vacía.** Ningún módulo tocado cabe en `tope_local` 120: scene-normalize 229, mundo-persistido 308, entidades-del-tile 139, scene-schema 170. Y `scene-expand.ts`, `schemas.ts`, `terrain-collision.ts` están en `sin_mutar`: un cambio ahí sale verde sin medir.
- **#358**: `main.ts` congelado en 2321; 4 casts a sustituir. Si baja, `client-file-size.json` baja en el mismo commit (trinquete). Sin conflicto. **#359**: sin conflicto (la puerta existe). **#293**: #397 es una instancia, coherente. **#302**: el `.strict()` del loader es revalidación de lo que se carga; va en la misma dirección.
- Solapamiento interno: #399 mata `makeScene` de `scene-expand.test.ts` y el test «walls collide» (ya cubierto en `terrain-collision.test.ts:160,209` con `origin`). #400.1, si se hiciera aquí, movería las coordenadas esperadas de la batería que #378 acaba de tipar: hacerlos juntos paga dos veces.

## Coste contra valor
#378: medio (un tipo compuesto de piezas que YA existen —`WorldRect`, `TerrainGridData`, `TilePlan`—, 15 casts, 6 firmas, 3 tipos de wire) y vale: cada tanda de T5/T6 pagó la conversión a mano. #399: barato, obligado por la regla de la casa. #400.2+.3: mecánico y ancho (~120 ediciones + Python), y vale porque hoy el motor escribe un campo que nadie lee y el `.strict()` de entity ya demostró que sirve. #397: medio; sin él el mismo modelo escribe `description` con dos semánticas. #400.1: no hacerlo hoy no cuesta nada — la rama tiene sujeto en tests y loader; hacerlo mal cuesta la batería entera.

## Corte propuesto (no diseño: alcance)
**PR-A** = #399 + #400 (restos 2 y 3): encoger el contrato de ENTRADA en los cinco procesos (zod, tool, prompts, Python, fixtures, tests, candado). Un tema, una corrida completa. **PR-B** apilada = #378 sobre la forma ya encogida. **PR-C** = #397, en paralelo con B (no toca `WorldScene`). #400.1 → issue nuevo con los tres sujetos medidos arriba; se cierra cuando `tile` sea obligatorio en el loader.

## Qué le cambiaría a `requisitos.md` (pegar tal cual)
- Alcance 1 (#378), añadir: «El tipo declara `exits` como overlay del wire o como miembro, pero lo decide el tipo (`wire-scene.ts:105`, `carga-de-tile.ts:143,164`); `SessionData.scenes_loaded` en el wire deja de tiparse como Format D (`types.ts:87`, `wire-scene.ts:143`); `__format_d` deja de viajar entero: su único lector es `place_id` (`main.ts:2029`) y pesa el 44 % del tile.»
- Alcance 2 (#397), sustituir el criterio por: «`name` obligatorio y rotulado, `description` opcional, en los CINCO sitios: `schemas.ts`, `narrative/types.ts:131-143,356-364`, `consequence-handler.ts:95-135` (sin default mudo), `mundo-persistido.ts:436-443`, `narrative_schemas.py:1048-1056`. El save YA conserva las dos: el guion solo lo demuestra. El envoltorio `scene_init` (`context.ts:334-340`) se adapta o se saca de `spawn_entity`.»
- Alcance 3 (#399), añadir: «Reapuntar `qa/contrato-candados-en-negativo.mjs:65-69`; decidir `W` en `DEFAULT_SOLID_CHARS` (0 productores) en la misma PR o abrir derivado; `tile_instructions.md:321` y `CLAUDE.md:183` entran en el barrido.»
- Alcance 4 (#400), sustituir: «Restos 2 y 3. `.strict()` en `EmittedSceneSchema` Y `ExpandedSceneSchema` (`:245`, `:341`) declarando `ambient_event`, `place_anchors` y, solo en el loader, `place_id`. `glyph` fuera de 17 JSON, 25 tests, Python (`:769-779` + 3 tests), tool, 2 prompts, labs y CLAUDE.md. El resto 1 sale de T7 a issue propio.»
- Alcance 6: «`npm run mutacion -- local` no aplica a ningún módulo de la tanda; todo se pide. Cada PR fuerza corrida completa (#404): se dice en el comentario de medida.»
- A1: añadir `nefan-html/src/main.ts` y `ui/style-apply.ts` al `grep` (hoy 4 y 2 casts) y «`WorldSceneDelBatch` desaparece». A5: quitar «las ramas centrada en el origen no existen».
