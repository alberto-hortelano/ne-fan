# Jornada autónoma del 2026-08-25

Documento para el usuario, que está ausente. Se actualiza sobre la marcha. Lo de abajo del
todo es lo más reciente.

> Mergea y sigue con el backlog, elige y prioriza proximas tareas y continua de forma
> autonoma, yo voy a estar fuera unas horas, haz cosas que no necesiten de mi feedback y lo
> que surja lo dejas apuntado para que lo vea al final.

## Cómo he priorizado

Filtro de entrada: **nada que necesite tu ojo, tu cartera ni tu decisión**. Eso deja fuera de
esta jornada tres cosas que están listas para cuando vuelvas:

| Fuera de la jornada | Por qué |
|---|---|
| Regenerar `nefan-html/public/sprites` | Aprobado por ti con la condición «verlo antes de decidir en firme». Produce arte: es tu ojo |
| `./start.sh --preset play` con créditos | 4,08 $ el primer personaje. Es tu cartera |
| #216 (7 modelos base, 1 con hojas) | Depende de lo anterior |

Dentro entra lo que se puede **medir**, y por orden de daño a quien juega:

1. **Reanudar te devuelve donde lo dejaste** (#245 #249) — el peor bug abierto: reanudas y
   apareces en (0,0,0), que en el tile del bench cae dentro de la taberna. Pantalla negra.
2. **El bosque es uno solo** (#243 #233 #232) — la misma `vegetation_zones` planta dos veces y
   solo una colisiona; el bridge no deriva volúmenes, así que los NPCs atraviesan lo que a ti
   te frena.
3. **Los candados que no pueden ponerse rojos** (#231 #248 #247) — tres verdes que no
   significan nada. Es el modo de fallo que ya nos ha costado dos tandas.

## Bitácora

### Mergeado

- **PR #258** — la retirada del gpu-worker, `c007e60`. #199 se cerró solo.

### Hecho a mano (prosa, directo a `main` por tu regla de cambios menores)

- **`006f4f8` — #255, punto 1.** Un clon limpio no tiene personajes y el repo no decía cómo
  conseguirlos. Al documentarlo apareció que **el documento que había no existe para quien
  clona**: `assets/characters/mixamo/README.md` cae dentro de `/assets/characters/`, ignorado
  entero. Y estaba obsoleto por dos sitios (mandaba los FBX a `godot/`, retirado en agosto; y
  afirmaba que «el juego funciona sin estos assets»). Ahora vive en
  `docs/assets-de-personaje.md`, versionado, citado desde el README raíz.
  **El comando está ejecutado, no supuesto** — y de ahí sale un dato: `--all` **no sirve** de
  primer paso (320 hojas contra un tope de 32). El punto 2 del issue sigue abierto.

### Las tres críticas volvieron, y las tres reencuadran

Ninguna tarea era obsoleta; las tres eran **más grandes de lo que decía su issue**. Los seis
issues están actualizados en GitHub con la medida delante.

**El bosque no era el sujeto.** El crítico tumba mi lectura y la mejora: el problema es que
**una entity estática del esquema y su volumen del plan son dos representaciones del mismo
objeto que nadie reconcilia** — el cliente pinta las dos, solo el volumen colisiona y el bridge
no ve ninguna. Los 8 árboles declarados de la fixture llevan un poste de 1×4×1 m **dentro de su
propia copa**, y los props igual. Y no es cosa de una fixture: `cuentos_oscuros/world/tile.json`
trae **1.049 postes de 1.052 entities**; `alta_fantasia`, 480 de 491. Los dos mundos base
arrancan en un campo de postes atravesables.

De #232 el titular correcto tampoco eran los árboles: de las 620 celdas en que divergen el plan
del cliente y el del bridge, **566 son edificios**. Los NPCs se meten dentro de las casas.

Retirar la ruta B **cuesta cero euros** — medido, no supuesto: con y sin las `scattered` salen
los mismos 26 volúmenes y las mismas 11 identidades de celda de atlas.

**#245 no era un mensaje que falta, es un campo que no se refresca.** Verificado en vivo hoy
(guion 17: empieza en `{0.25, 0, 3.25}`, reanuda en `{0,0,0}`). El bridge ya escribe `state.json`
**trece veces** y ya tiene la posición delante; lo único que no refresca antes de escribir es
`NarrativeState.player.position`. Así que `save_session` se retira entero en vez de buscarle
emisor. Al alza: **hoy reanudar te cura siempre a 100 HP**, por la misma causa.

**#231 se parte en dos, y su mitad (b) va detrás del bosque**: 19 de sus 59 errores de tipos
viven en los tests del módulo que el bosque reescribe. Arreglarlos ahora sería arreglarlos dos
veces. La deuda de tipos en `test/` crece ~1,75 errores por tanda: eran 59, hoy son 66.

**Orden de la cola, decidido**: `#248` → `#231(a)` → bosque → `#231(b)` → `#247`.

### Issues nuevos abiertos hoy

- **#260** — 6 `no-misused-promises` en `nefan-html`: promesas entregadas a quien las ignora.
  El único subconjunto de las 38 violaciones de tipos que puede morder en runtime.
- **#261** — **el candado que prohíbe esperar por reloj está verde sobre la espera por reloj de
  #247.** Cuarto caso del patrón, y encontrado gratis: su patrón caza el `sleep` literal, y un
  `waitFor` cuya condición no se cumple nunca es un sleep con mejores modales.
- **#262** — el mercader que huye avanza **1 m en 30 s** con `run_speed 2.8` hacia un objetivo a
  16 m. El guion 15 lleva desde que nació reportándolo y se ha leído como intermitencia del
  guion. Bloquea de hecho a #247: no se puede elegir su aserto sin esta respuesta.

### En marcha

Un ingeniero (telegraph #184 #185) y tres arquitectos (estáticos, reanudar, bosque).

## Para cuando vuelvas

*(se rellena al final: descartados, reencuadrados y lo que exija decisión tuya)*
