# Crítica — T13 «La deuda que dejaron las tandas» · `main` = `e62a750` · 2026-09-05

**VEREDICTO DE CONJUNTO: siete de los ocho tienen sujeto vivo; uno (#451) sale de la tanda hasta que el usuario decida; dos (#405, #408) se hacen MÁS PEQUEÑOS de lo que dicen; una relación de `requisitos.md` (#405→#411) es falsa y cambia el corte.**

| Issue | Veredicto | En una línea |
|---|---|---|
| #451 | **PREMATURA** (fuera de la tanda) | Premisa cierta; el arreglo es una feature que exige decisión del usuario, no una corrección |
| #452 | **VIGENTE** | `applyToState` escribe el array entero sin forma; `loadSession` no mira el inventario |
| #453 | **VIGENTE** (con un matiz) | «Sin sesión» = `session_id === ""`; la sesión *provisional* (#279) NO es sin sesión y no debe volverse 500 |
| #405 | **REENCUADRADA** (menor) | Un sujeto ya murió (`scene-expand.test` va a 128×128 con `tile`); el camino no-grid del cliente no tiene fixture que lo ejerza |
| #407 | **VIGENTE** (remedido) | 47 literales en 5 tests, no 52; `scene-expand` ya tiene 0; sale de la lista, no le entra productor |
| #408 | **REENCUADRADA** | «Darle productor» ya existe por OTRO canal (`map_upsert_place.anchor`): retirar, sin dilema |
| #410 | **VIGENTE** (acotado) | Solo `exits` es overlay del wire; `position_declared`/`combat` los emite core. Sin harness de cliente (#241) el test no tiene dónde vivir |
| #411 | **VIGENTE** (independiente) | Las dos llamadas son `main.ts:807` y `style-apply.ts:205`, no `loadRoom`: #405 no baja el `max` |

## Corte propuesto de la tanda

- **Paralelos e independientes**, una PR cada uno (no comparten fichero): **#453** (`state-http-server.ts`, `dispatch.ts`), **#452** (`plugins/dispatcher.ts`, `narrative-state.ts:565`), **#411** (`arch-rules.json`; puede ir sola y hoy), **#407** (44 de los 47 literales viven en `scene-validate-pasadas` y `terrain-collision`, tests que #405 NO reescribe), **#408** (retirada en cinco procesos).
- **Secuencia #405 → #410**: comparten `nefan-html/src/world/tile-store.ts` (#405 quita `tx?`/`ty?` y el `getAt` por rect, `:24-26,70-77`; #410 cambia `add()`, `:88`). Misma PR o dos apiladas; #410 antes de #405 pagaría dos veces el tipo `TileClientState`.
- **#451 fuera** hasta la respuesta del usuario (abajo). Los dos «menores» que cita (motivo del `stale` en el título; `ok:true/stale` vs `ok:false`) sí son correcciones y podrían ir con #453 si el usuario quiere aprovechar el contexto del bridge.

## #451 — PREMATURA

Problema real: una escena injugable de nueve invalida y **reescribe** el snapshot de las otras ocho. Premisa verificada: `src/games/world-snapshot.ts:138-147` lanza a la primera escena que no pasa; `bridge/handlers/session.ts:476-489` degrada al bootstrap vivo; `bootstrap-tile.ts:114` → `writeSessionSnapshot` (`context.ts:154-167`) vuelca `scenes_loaded`, que tras un bootstrap fresco es UNA. No hay medio arreglo barato: no reescribir el snapshot al degradar solo aplaza la misma degradación a la siguiente partida. **Decisión que hace falta, del usuario:** «Cuando un snapshot trae N escenas y k fallan el validador de hoy: (a) servir la entrada y regenerar solo las k por la cola —feature, toca `loadWorldSnapshot`, `replayWorldSnapshot` y `sceneGen`—; (b) rechazar el snapshot entero pero NO sobreescribirlo hasta que el nuevo mundo tenga ≥ N escenas; (c) dejarlo como está y cerrar el issue como decisión.» Si no hay respuesta, no entra.

## #452 — VIGENTE

Premisa: `dispatcher.ts:278-284` (`PLAYER_WRITABLE` incluye `inventory`), `:291-318` (`applyToState` asigna `write.value` tal cual), `dsl/evaluate.ts:102-105` (el valor es el ARRAY ENTERO tras los efectos, así que un `push` de `{name:"x"}` entra completo). `loadSession` valida escenas (`narrative-state.ts:514`), posiciones (`:531`) y nombres (`:545`) y hace spread de `player` sin mirar el inventario (`:565`). El molde ya existe: `InventoryAddRequestSchema` (`src/contracts/request-schemas.ts:146`). Día después: el único plugin vivo que escribe inventario (`data/games/toledo_1200/plugins/commerce.json:56`) ya emite `{id, from}` → nada cambia para quien juega hoy; se cierra la puerta al plugin futuro. Conflicto con #361: ninguno si el gate se pone POR CAMPO en `externalWriteAllowed`/`applyToState` y no como rediseño de la whitelist. Coste bajo, valor: hace verdadera la promesa del tipo que #449 acaba de introducir.

## #453 — VIGENTE, con un matiz que el arquitecto necesita

Premisa: `state-http-server.ts:96-101` traga el `throw` de `onMutation` con `warn` y responde el 200 del handler; `ws-server.ts:179-181` → `narrative.save()`, que **lanza** con `session_id` vacío (`narrative-state.ts:596-598`). El guardia de sesión deja pasar `""` a propósito (`dispatch.ts:77`). **¿Debe el State API aceptar mutaciones sin sesión? No**, y la casa ya lo dice en otro sitio: las rutas de documento responden 404 sin sesión (`doc-routes.ts:32,51,73,86`; test `state-http-dispatch.test.ts:348`). Quién llama sin sesión hoy: el MCP solo manda cabecera con sesión activa (`narrative-mcp/bridge-http-client.ts:84`) y el banco muta dentro de una generación con sesión abierta (`labs/narrative/fake-ai-server.ts:178-191`) → nadie legítimo. **El matiz:** la sesión *provisional* de #279 (`narrative-state.ts:378`; la efímera de `game-gen.ts:170`) tiene `session_id` y `save()` devuelve `{escrito:false}` SIN lanzar (`:600`) — por diseño, su artefacto es el snapshot. «Sin sesión → 409» debe medir `session_id === ""`, y «`onMutation` lanza → 500» no debe tocar ese caso. Test en negativo: mutación con sesión provisional sigue en 200.

## #405 — REENCUADRADA (menor de lo que dice)

Sujeto 1 remedido: `test/scene-expand.test.ts:20-47` ya construye `makeTile()` 128×128 con `tile` (murió con T7-T11). Siguen vivos `scene-normalize.test.ts:26-40` (`makeFormatD` 10×6 sin `tile`; 2 `tile:` en 43 tests) y `helpers.ts:84-97` (`escenaExpandidaDePrueba` 4×4 sin `tile`, 10 ficheros consumidores); 8 ficheros de test construyen escenas expandidas sin ningún `tile:` (`retired-terrain-fields`, `plugin-register`, `narrative-state`, `architecture`, `bridge-session`, `tile`, `hostiles`, `consequence-handler`). Sujeto 2 cierto: `tile` opcional en `sceneBaseShape` (`scene-schema.ts:184`), `ExpandedSceneSchema` (`:385-395`) no lo exige; `WorldScene.tile?` (`scene-normalize.ts:76`); ramas en `scene-normalize.ts:204-212`, `terrain-collision.ts:107,160`, `tile-plan.ts:83,98`, `sim-collision.ts:82`. El comentario `types.ts:90-92` **ya no existe**. Sujeto 3 (cliente): el camino `!isGridTile` sigue (`carga-de-tile.ts:272-279,331-334,360-363,372-374`; `tile-store.ts:24-26,70-77`) pero **no tiene fixture que lo ejerza**: las 3 de `data/scenes/` llevan `tile` (`grep -L` → 0); solo lo alcanza un save sin `tile`, que es lo que el sujeto 2 admite. `change_scene` vive solo en un comentario (`main.ts:805`); `tile.ts:5` cita «la migración del save v3», muerta con #336. #378 está CERRADO: la precondición se cumple. Coste real: reescribir `makeFormatD` a 128×128 @0,5 m cambia TODAS las coordenadas esperadas de 43 tests — es la PR cara de la tanda, y los otros dos helpers son mecánicos. Qué no hacer: no tocar `tile.ts:40-51` (el «centrado» del tile (0,0) es geometría viva, no la rama legacy).

## #407 — VIGENTE, remedido

`DEFAULT_SOLID_CHARS` está en `scene-normalize.ts:162` (no :91). Productores de `W`: cero (`grep` de la literal en `src/` → solo comentarios: `scene-normalize.ts:158`, `terrain-collision.ts:4`, `tile-edges.ts:37`, `scene-validate.ts:861` — cuatro, no dos). Literales: `scene-validate-pasadas` 30, `terrain-collision` 14, `sim-collision` 1, `plan-collision` 1, `scene-normalize` 1 = **47 en 5 tests**; `scene-expand` 0 (los 9 murieron con PR-A). Retirar, no dar productor: los muros del juego son volúmenes del plan (`S` en los grids derivados, `terrain-collision.ts:109`), no chars del grid. El único sólido con productor es `w` (agua, la rasteriza `expandScenePrimitives`); si algún test de `scene-validate-pasadas` distingue agua de muro (puentes `b`), ahí está el coste oculto — que lo mida el ingeniero antes de sustituir a ciegas. La relación «#405→#407 pagan la misma reescritura» de `requisitos.md` es débil: solo 2 de los 47 literales viven en tests que #405 toca.

## #408 — REENCUADRADA: no hay dilema

Premisa cierta: cero apariciones en `data/contract/prompts` y `tools`; lo escribe el banco (`labs/narrative/fake-scenes.ts:252,326`) y lo declara el espejo Python (`ai_server/narrative_schemas.py:116-121,644-658`). Lectores: `narrative-state.ts:685-696` (fija `place.anchor` con rect), `bootstrap-place.ts:53-63` (fallback cuando falta `place_id`, `:83-92`), comentarios `tile.ts:168`, `scene.ts:178`. **Lo que el issue no vio:** el mismo dato ya tiene productor real por otro canal — `map_upsert_place` expone `anchor {tx,ty,rect}` al motor (`narrative-mcp/server.ts:699-707`, `request-schemas.ts:80-84`, `world-map.ts:106`), y el viaje fija el anchor solo (`scene.ts:164-167`, `place-anchor.ts`). Dar productor a `place_anchors` sería un segundo canal para el mismo campo. Retirar. Lo que muere con él: el zod (`scene-schema.ts:216`), `EMITTED_SCENE_FIELDS` y su test «exactamente `place_anchors`» (`contract-prompts.test.ts:210-225`, pasa a «exactamente nada»), 3 fixtures `data/contract/fixtures/scene/invalid/ancla_*.json`, tests (`scene-schema` 10, `bootstrap-place` 8, `bridge-map` 4), el fallback de `bootstrap-place` (que queda en `place_id` corroborado → error), fake y Python; término en `campos-retirados-no-vuelven`. Es una decisión de diseño con opción dominante; pide el visto bueno del usuario en una línea: «`place_anchors` se retira; el motor ancla lugares con `map_upsert_place.anchor`, que ya existe. ¿OK?».

## #410 — VIGENTE, acotado

Premisa: `tile-store.ts:88` `JSON.stringify(tile.scene)` con `scene: EscenaServida = WorldScene & { exits }` (`messages.ts:248`); `actualizarSalidas` muta `entry.scene.exits` sin tocar `fingerprints` (`carga-de-tile.ts:159-171`); `sceneChanged` decide si se restaura `svgCollider` (`:325-328`). Corrección al issue: el overlay del wire es SOLO `exits` (`wire-scene.ts:106`); `position_declared` y `combat` los emite `formatDToWorld` y son geometría/estado que sí debe entrar en la huella. Sin sujeto medible hoy: nefan-html no tiene harness (#241 abierto), así que «test: dos escenas iguales con `exits` distintas tienen la misma huella» solo puede vivir en core (si la huella se calcula sobre `WorldScene` en core) o en un guion headless de `qa/`. Que el arquitecto elija con eso delante.

## #411 — VIGENTE e independiente

Regla verificada (`arch-rules.json`: `desc` «El cliente 2D…», `max: 2`, patrón `formatDToWorld\s*\(` sobre `nefan-html/src/**/*.ts`). Llamadas hoy: **`main.ts:807`** (`addTileRaw`, selector «Room» → `main.ts:754`) y **`ui/style-apply.ts:205`** (vista previa del snapshot en el título). `carga-de-tile.ts` NO llama a `formatDToWorld` (`:218` es un comentario). Por tanto `requisitos.md` se equivoca: #405 no retira ninguna de las dos y el `max` se queda en 2 (o dos reglas por ruta con `max: 1`). Puede hacerse hoy, sola, en diez líneas de JSON.

## Qué cambiar en `requisitos.md` (pegar tal cual)

- Sustituir la relación «**#405 → #411**» por: «#411 es independiente: las dos llamadas legítimas son `nefan-html/src/main.ts` (`addTileRaw`, fixtures del selector) y `nefan-html/src/ui/style-apply.ts` (vista previa del snapshot). #405 no retira ninguna; `max` no baja.»
- Sustituir «**#405 → #407**» por: «#407 es independiente: 44 de sus 47 literales viven en `scene-validate-pasadas` y `terrain-collision`, que #405 no reescribe. `scene-expand` ya tiene 0.»
- En #405 añadir: «`scene-expand.test.ts` ya está en 128×128 con `tile` (no cuenta). Quedan `makeFormatD` (`scene-normalize.test.ts`), `escenaExpandidaDePrueba` (`helpers.ts`, 10 consumidores) y 8 ficheros sin `tile:`. El comentario `types.ts:90-92` ya no existe. No tocar la geometría «tile (0,0) centrado» de `tile.ts`.»
- En #408 sustituir «darle productor o retirarlo» por: «se retira: el motor ya ancla lugares con `map_upsert_place.anchor {tx,ty,rect}` (narrative-mcp/server.ts:699). Pendiente del visto bueno del usuario.»
- En #453 añadir: «“sin sesión” es `session_id === ""`. La sesión provisional (#279, `game-gen.ts:170`) sigue mutando en memoria con 200: `save()` devuelve `{escrito:false}` sin lanzar y así debe seguir. Test en negativo obligatorio.»
- En #410 añadir: «solo `exits` es overlay del wire; `position_declared`/`combat` entran en la huella. El test vive en core o en `qa/` headless: el cliente no tiene harness (#241).»
- Mover **#451** a «pendiente de decisión del usuario» con la pregunta (a)/(b)/(c) de arriba; la tanda son siete.
